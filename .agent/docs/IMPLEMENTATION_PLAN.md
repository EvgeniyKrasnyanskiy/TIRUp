# ПЛАН РЕАЛИЗАЦИИ: Комплексные улучшения TIRUp (Фаза 66) [Completed]

## Анализ и техническая декомпозиция задач пользователя

---

### План работ по этапам: [Все этапы завершены]

#### Этап 1: Исправление критических дефектов (SMS, батарея мастера, шторка)
1. **SMS автоответ (п. 9)**:
   - В `AndroidManifest.xml` для `SmsQueryReceiver` удалить `android:permission="android.permission.BROADCAST_SMS"`.
   - В `SettingsScreen.kt` добавить кулдаун/таймер блокировки (60 секунд) на кнопку «Отправить тестовое SMS» с визуальным обратным отсчетом на кнопке.
2. **Шторка уведомлений (п. 8)**:
   - В `GlucoseAlertManager.kt` скорректировать заголовок предиктивного уведомления: `Прогноз: ГИПО⬇️ ~HH:mm` и `Прогноз: ГИПЕР⬆️ ~HH:mm` (строго ≤20 символов).
3. **Индикатор батареи мастера (п. 10)**:
   - В FocusScreen.kt убрать иконку смартфона `📱`, оставив только `🔋 X%`.
   - Порог устаревания `isMasterBatteryStale` уменьшить с 7 минут до 5 минут (синхронно со старением сахара).
   - При устаревании принудительно делать цвет текста и рамки нейтрально-серым (`Color(0xFF94A3B8)`), отображая `🔋 ?`.

#### Этап 2: Восстановление данных IoB и CoB (п. 6)
1. Проверить цепочку поступления IoB/CoB в DexdripBroadcastReceiver.kt:
   - Проверить экстра-поля броадкастов xDrip/GDH (`iob`, `cob`, `pebble`, `wearintegration`).
   - Убедиться, что при асинхронном запросе к порту 17580 (`fetchIobCobFromLocalPebbleService()`) обновленные данные не перезаписываются нулями при повторном броадкасте дедупликации.
   - Проверить передачу `iob` и `cob` в карточку героя FocusScreen.kt и тултип графика DailyGlucoseChart.kt.

#### Этап 3: Тревоги и алгоритм подавления (п. 1, п. 11)
1. **Информационное окно по тапу на плашку тревоги (п. 1)**:
   - Сделать карточку активной тревоги кликабельной (`Modifier.clickable`).
   - По тапу открывать диалоговое окно с детальным описанием текущей тревоги (уровень, порог, число подтверждающих точек, почему сработала, когда повторится, что предпринять).
2. **Корректировка описаний в настройках и PDF-руководстве (п. 1)**:
   - Синхронизировать тексты: для 5-минутных сенсоров основная тревога проверяет 3 точки (15 мин), для 1-минутных — 5 точек (5 мин).
   - Потеря связи: описать, что интервал срабатывания составляет 20–25 минут (с учетом 5-минутного шага сенсора и проверки планировщика).
3. **Умное подавление основной тревоги при гипергликемии (п. 11)**:
   - В GlucoseAlertManager.kt для `AlertTier.MAIN` (высокий сахар): если сахар выше целевого, но скорость отрицательная (тренд идет вниз: `↘`, `↓`, `⇊` или delta < 0) И в организме есть активный болюс `IoB >= 0.2U` (или `>= 0.5U` при сахаре > 13.9), первичный сигнал тревоги подавлять (не будить/не тревожить звуком, фиксируя статус в лог), так как компенсация уже введена и действует.

#### Этап 4: Таймеры и компактный UI расходников + Ланцет (п. 2, п. 3, п. 12)
1. **Поддержка ланцета и чекбоксы устройств в настройках (п. 12)**:
   - Добавить модель и состояние для ланцета (срок замены от 1 до 7 дней).
   - В настройках «Напоминания об устройствах» сделать чекбоксы в одну строку: [x] Сенсор, [x] Инф. набор, [x] Ланцет.
   - Убрать зависимость отображения инфузионного набора от помпы в профиле пользователя — отображение регулируется чекбоксами.
   - Добавить пиктограмму ланцета (маленькая, например 💉 или 🩸 или 📍).
2. **Выравнивание размеров кнопки расходников и стрика (п. 2)**:
   - В DeviceStatusChips.kt уменьшить размер пиктограмм с `30.sp` до `13-14.sp` (соразмерно тексту), внутренние отступы выровнять по высоте со `StreakBadge`.
3. **Формат таймера <24 часов и просрочка (п. 3)**:
   - Если до замены осталось < 24 часов: вместо `0д` выводить часы `23ч, 22ч...` (если < 1 часа — минуты `59м...`, если < 1 мин — секунды).
   - Если срок истек: подсвечивать плашку и кнопку красным цветом `MaterialTheme.colorScheme.error`, менять текст на `Замените ...` или `Просрочен: -Xч/д`.
   - В сервисе уведомлений не писать `Осталось 0д`, а отображать оставшиеся часы/просрочку.
4. **Компактность экрана устройств**:
   - Сделать модальное окно DeviceStatusModal.kt компактнее по высоте с поддержкой ланцета.

#### Этап 5: Оптимизация хинтов, BLE-мост и анимации (п. 4, п. 5, п. 7)
1. **Всплывающие подсказки (п. 4)**:
   - Убрать постоянный верхний оверлей-баннер `TIRUp: ...` на главном экране.
   - Нижний баннер оставить исключительно для тестового ping в настройках.
   - Для приемника на карточке героя добавить анимированную пиктограмму входящего сигнала (с волнами внутрь к центру), активирующуюся только в момент приема пакета.
2. **Быстрое управление BLE-мостом (п. 7)**:
   - В диалог по тапу на пиктограмму BLE-моста добавить кнопку быстрого отключения/включения BLE-моста. При выключении пиктограмма перечеркивается/становится серой.
3. **Анимация перерисовки сахара (п. 5)**:
   - Добавить плавную анимацию изменения чисел (AnimatedContent / crossfade) при обновлении сахара, тренда и дельты на главном экране.

