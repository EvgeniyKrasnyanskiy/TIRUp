# План реализации поддержки единиц mg/dL и полной локализации (EN/RU)

## 1. Поддержка единиц mg/dL в калькуляторе метрик и моделях
- **Проблема**: При переключении в настройках на `mg/dL` калькулятор `GlucoseMetricsCalculator` и виджеты продолжали оперировать значениями `ммоль/л`, порогами `≤7.8` и диапазонами `3.9–10.0`.
- **Задачи**:
  1. Добавить параметр `unit: GlucoseUnit = GlucoseUnit.MMOL_L` в `GlucoseMetricsCalculator.calculateStatistics` и `generateClinicalSummary`.
  2. В `generateClinicalSummary` динамически подставлять единицы и пороги:
     - При `GlucoseUnit.MG_DL`:
       - Mean BG: `137 mg/dL` (цель/target `≤140 mg/dL`).
       - TIR: `TIR (70–180 mg/dL)` (цель `≥70%`).
       - TING: `TING (70–140 mg/dL)` (цель `≥50%`).
       - TBR: `TBR < 70 mg/dL` и `TBR < 54 mg/dL`.
       - TAR: `TAR > 180 mg/dL` и `TAR > 250 mg/dL`.
       - SD: `43 mg/dL` (цель `≤54 mg/dL`).
       - Ночной сон: `SD ≤27 mg/dL`.
     - При `GlucoseUnit.MMOL_L`:
       - Стандартные пороги и значения в `ммоль/л` / `mmol/L`.
  3. Передавать текущий `unit` из `UserSettings` во всех ViewModel (`ReportsViewModel`, `FocusViewModel`, `TrendsViewModel`).

## 2. Полная локализация и поддержка mg/dL во вкладке «Отчёты» и PDF
- **Проблема**: В `ReportsScreen.kt`, `AgpSheetPreviewModal.kt` и `AgpPdfGenerator.kt` многие заголовки, подписи кнопок («Сохранить», «Поделиться», «Загрузить файл») и карточка метрик были жестко захардкожены на русском языке и в `ммоль/л`.
- **Задачи**:
  1. В `ReportMetricsColumn` (`ReportsScreen.kt`) динамически переключать единицы (мг/дл vs ммоль/л) и язык (`RU` vs `EN`).
  2. Все кнопки и заголовки карточек в `ReportsScreen.kt` сделать двуязычными (`isRu`).
  3. В превью модального окна `AgpSheetPreviewModal.kt` перевести все разделы и кнопки на выбранный язык.
  4. В `AgpPdfGenerator.kt` при формировании PDF проверять `userSettings.unit` и генерировать диапазоны и средний сахар в мг/дл, если выбран `mg/dL`.

## 3. Полная локализация и поддержка mg/dL на главной вкладке «Профиль» (Focus)
- **Проблема**: В `FocusScreen.kt` числа «Средний сахар», «Мин/Макс за сутки» и диалоговые окна с описанием метрик оставались в ммоль/л и на русском языке.
- **Задачи**:
  1. Конвертировать отображаемые числа Mean BG, Min BG, Max BG в целые числа мг/дл при `unit == GlucoseUnit.MG_DL`.
  2. Перевести все подсказки и заголовки бенто-карточек на английский при `language == "EN"`.
  3. Полоса компенсатора цели: сделать толще (`18.dp`), ярче и контрастнее с акцентным фоном трека.

## 4. Онбординг, медицинский дисклеймер и справка по связке с xDrip+
- **Задачи**:
  1. Добавить `hasSeenOnboarding: Boolean` в `UserSettings` и `SettingsRepositoryImpl`.
  2. Создать компонент `HelpAndDisclaimerDialog`:
     - Медицинский отказ от ответственности (дисклеймер).
     - Пошаговое руководство по настройке xDrip+ («Межпрограммная интеграция» -> «Широковещательный показ данных»).
     - Импорт исторических баз данных (ZIP/CSV) для формирования AGP-отчётов.
     - Двуязычная поддержка (`RU` / `EN`).
  3. Показывать диалог автоматически при первом запуске (`!hasSeenOnboarding`).
  4. Добавить иконку `?` (`HelpOutline`) в шапку экрана настроек `SettingsScreen.kt` для вызова этой справки в любой момент.

## 5. Документация и синхронизация с Git [Completed]
- **Задачи**:
  1. [x] Подготовить подробный файл `README.md`.
  2. [x] Настроить remote репозиторий: `git@github.com:EvgeniyKrasnyanskiy/TIRUp.git`.
  3. [x] Закоммитить изменения и выполнить `git push -u origin main`.

## 6. Доработка интерфейса, контрастности и синхронизации локализации [Completed]
- **Задачи**:
  1. [x] **Локализация вида терапии и типа диабета**:
     - Добавлены функции `localizeTherapyType(therapy, isRu)` и `localizeDiabetesType(diabetes, isRu)`.
     - Применены в `SettingsScreen.kt`, `AgpSheetPreviewModal.kt` и `AgpPdfGenerator.kt`.
  2. [x] **Цвета и контрастность**:
     - В `SettingsScreen.kt` сделан `LanguageChip` синим (`ActionBlue`).
     - В `AgpSheetPreviewModal.kt` сделан тулбар адаптивным под светлую тему.
     - В `Theme.kt` усилена контрастность `onSurfaceVariant` (`#1E293B` светлая, `#E2E8F0` тёмная).
     - Исправлена бледность второстепенных надписей на всех экранах (Reports, Settings, Heatmap).
  3. [x] **UI экранов Focus и Trends**:
     - `FocusScreen.kt`: заменён дублирующийся показатель «Точек за сегодня» на «Разброс (SD)».
     - `TrendsScreen.kt`: перенесена TING/TIR цель на новую строку; переделаны 3 карточки по Варианту А (1 широкая + 2 средние).
  4. [x] **Нижняя панель управления (Dock)**:
     - В `MainActivity.kt` перенесена форма в `Surface(shape = RoundedCornerShape(24.dp))` с чёткой окантовкой, увеличены кнопки до `28.dp` и добавлена подсветка активной вкладки.
  5. [x] **Иконка приложения**:
     - Сгенерированы адаптивные и стандартные ассеты для всех плотностей экранов (`mipmap-*`).
  6. [x] **Классический детерминированный прогресс-бар импорта**:
     - Расчёт точного прогресса потока байт с отображением полосы, живого счётчика точек и процентов (`0%..100%`).
  7. [x] **Суточная математика компенсатора цели (24 часа)**:
     - Расчёт в часах и минутах внутри суток (`16 ч 48 мин` нормы TIR), динамические рекомендации (в диапазоне / вне диапазона), детальный суточный отчёт по тапу.

---

## 7. Дорожная карта и предложения по дальнейшему развитию проекта (Roadmap)

### 7.1. Виджет рабочего стола и экрана блокировки (Homescreen Widget)
> **Инициатива пользователя [Высший приоритет / Planned]**

**Концепция**:
Обеспечить мгновенный просмотр ключевых жизненных показателей прямо с домашнего экрана смартфона без необходимости каждый раз открывать приложение.

**Технологический стек**:
- **Jetpack Glance** (современный декларативный Compose-фреймворк для Android AppWidgets) или классический `RemoteViews`.
- Автоматический триггер обновления из `XdripBroadcastReceiver` каждые 5 минут сразу при получении нового замера.

**Варианты компоновки**:
1. **Компактный виджет (2x1 или 2x2)**:
   - Крупное текущее значение сахара в выбранных единицах (`ммоль/л` или `mg/dL`).
   - Стрелка тренда динамики (↑, ↗, →, ↘, ↓) с цветовым акцентом диапазона.
   - Дельта изменения сахара за последние 5 минут (например, `+0.4` или `-1.1`).
   - Время последнего замера («3 мин назад») с индикатором свежести сигнала.
2. **Расширенный функциональный виджет (4x2 — Bento Widget)**:
   - Все параметры компактного виджета.
   - **Суточный компенсатор цели**:
     - Заполняющаяся полоса TIR за сегодня (например: `82% / цель ≥70%`).
     - **Точная рекомендация в часах и минутах**: «В норме ещё 2 ч 15 мин» или «Вне нормы! Вернитесь в диапазон (нужно 3 ч 40 мин)».
   - Мини-график сахара за последние 3–6 часов (sparkline-график).
   - Быстрые действия: тап по сахару открывает `FocusScreen`, тап по компенсатору — подробную суточную справку.

---

### 7.2. Постоянное уведомление в шторке (Ongoing Notification & Always-On Display)
- Несмахиваемое компактное уведомление в шторке уведомлений и на Always-On Display:
  - Текущий сахар прямо в строке состояния (Status Bar).
  - Сводка одной строкой: `TIR 78% • удерживайте в норме ещё 2 ч 15 мин`.
  - Мгновенное обновление каждые 5 минут без задержек.

---

### 7.3. Интеграция с платформой Android Health Connect
- Двусторонняя синхронизация с защищённым медицинским хранилищем Android.
- Автоматическая передача уровня глюкозы крови в **Google Fit, Samsung Health, Whoop, Garmin**.
- Чтение данных о сне и физической активности для сопоставления с гликемией (влияние тренировок на ночной TIR).

---

### 7.4. Анализ факторов компенсации: еда и инсулин (Treatments & Meals Overlay)
- Приём broadcast-событий от xDrip+ о введённом инсулине (болюсы/базал) и съеденных углеводах (ХЕ / граммы).
- Наложение меток приёма пищи 🍽️ и инъекций 💉 на суточный график `FocusScreen`.
- Автоматический расчёт постпрандиального пика сахара через 1 и 2 часа после еды.

---

### 7.5. Умный клинический советник (Smart Pattern Recognition)
- **Детекция ночных гипогликемий**: выявление повторяющихся ночных провалов в определённые часы (например, 03:00–04:00).
- **Оценка скорости изменения сахара (Delta Rate)**: предупреждение при резком росте (>0.15 ммоль/л/мин), помогающее оценить правильность паузы перед едой.
- **Еженедельный дайджест прогресса**: сравнение текущей недели с предыдущей («TIR вырос на +4%, вариабельность CV снизилась на 3%»).

---

### 7.6. Приложение и циферблатные расширения для Wear OS (Smartwatch Complications)
- Слот-расширение (Complication) для циферблатов Wear OS (Galaxy Watch, Pixel Watch): текущий сахар + стрелка + мини-кольцо суточного TIR.
- Компактный экран часов с суточным компенсатором.

---

### 7.7. Дистанционный родительский контроль и обмен отчётами (Remote Care)
---

## 8. Редизайн экранов Focus, Trends, Настроек, Памятки и Детекции паттернов [Completed]
- **Задачи**:
  1. [x] **Экран «Текущие сутки» (`FocusScreen.kt`)**:
     - Переименован экран в «Текущие сутки» (`nav_focus`: «Текущие»).
     - Заголовок текущего сахара переименован в «Глюкоза», убран неточный счётчик точек.
     - Заголовок «Компенсация», переключатель TIR/TING перенесён в правый верхний угол карточки, цели вынесены на вторую строку под заголовок.
     - Фрейм «Ночной профиль» поднят наверх, сразу за компенсацией цели.
     - Добавлена компактная сетка метрик 2x4 (Mean, SD, %CV, eA1c / GRI, GVI, PGS, Min/Max) с аккуратными отступами 8.dp и скруглением 16.dp.
     - Текст серии сокращён до «%d д.».
  2. [x] **Экран «Тренды» (`TrendsScreen.kt`)**:
     - Удалён переключатель TIR/TING из шапки экрана.
     - Создан алгоритмический модуль `PatternRecognitionEngine.kt` с unit-тестами.
     - Добавлен бенто-фрейм «Обнаруженные паттерны» (детекция ночных гипогликемий, «утренней зари», постпрандиальных всплесков, дневной лабильности и безаварийного профиля).
  3. [x] **Экран «Настройки» (`SettingsScreen.kt`)**:
     - Удалена тема «Авто» (остались «🌙 Тёмная» и «☀️ Светлая»).
     - Скрыты поля ручного ввода стандартизированных порогов (Верх TING, Цель TIR, границы TIR).
     - Добавлен информационный бейдж стандартов ATTD / ADA.
     - Сохранены селекторы времени сна («Начало сна» и «Конец сна»).
  4. [x] **Экран «Отчёты», Памятка и PDF (`ReportsScreen.kt`)**:
     - Цвет среднего сахара 7.6 ммоль/л на превью отчёта изменён на мягкий салатово-жёлтый (`ColorTargetSoft` `#84CC16`).
     - Добавлена карточка «📖 Справочник» внизу экрана отчётов.
     - Реализовано модальное окно с подробным описанием каждого параметра и дисклеймером CGM (MARD 8–10%, задержка 5–15 мин, контрольный замер по капле крови).
     - Создан `GuidebookPdfGenerator.kt` для генерации и отправки отдельной печатной памятки в PDF.
  5. [x] **Качество и развёртывание**:
     - Все 26 модульных тестов успешно пройдены (`testDebugUnitTest`).
     - APK собран и установлен на смартфон через ADB (`Performing Streamed Install: Success`).

---

## 10. Разделение суточного и многодневного компенсатора, оптимизация Трендов и Настроек [Completed]
1. [x] **Экран «Текущие сутки» (`FocusScreen.kt`, `TargetCompensatorCalculator.kt`)**:
   - Реализован точный суточный расчёт (00:00 – 23:59).
   - Чёткая инструкция на остаток дня в часах и минутах: сколько часов прошло, сколько набрано в норме, сколько времени из оставшихся часов суток нужно удержать в диапазоне для закрытия цели ≥70% (TIR) или ≥50% (TING).
   - Устранено расхождение процентов и путаница с многодневным окном на экране текущего дня.
2. [x] **Экран «Тренды» (`TrendsScreen.kt`, `TrendsViewModel.kt`)**:
   - Внедрён фрейм «Стратегический компенсатор цели» под селектором интервала.
   - Рассчитывает количество успешных дней в периоде (напр., «11 из 14 дн.» с процентом успеха), суммарный баланс времени (запас или дефицит часов) и рекомендованный суточный TIR на следующий период.
   - Фрейм «Распределение по диапазонам» поднят наверх для мгновенного отклика при смене периода.
   - Внедрена компактная сетка 3x4 (12 ключевых клинических параметров).
   - Карточка «Обнаруженные паттерны» снабжена кнопкой «Скрыть» и скрывается при недостаточном объёме данных.
3. [x] **Генератор AGP PDF (`AgpPdfGenerator.kt`)**:
   - Временная ось переведена на шахматный двухзначный вид часов без минут (`00`, `03`, `06`, `09`, ...).
4. [x] **Экран «Настройки» (`SettingsScreen.kt`)**:
   - «Параметры отображения» вынесены наверх, остальные разделы спрятаны под элегантный сворачивающийся блок «Дополнительные настройки».
5. [x] **Контроль качества**:
   - Unit-тесты успешно пройдены (`testDebugUnitTest`).
   - APK собран и обновлён на устройстве (`adb install -r: Success`).

---

## 11. Редизайн Аналитики, Профиль пациента в Настройках, GRI (риск гипо) [Completed]
1. [x] **Экран «Аналитика» (`TrendsScreen.kt`, `strings.xml`, `AgpChart.kt`)**:
   - Переименован из «Аналитика трендов» в лаконичное «Аналитика» (Analytics).
   - Карточка «Амбулаторный профиль (AGP)» поднята выше сетки параметров 3x4.
   - Временная шкала на мобильном графике выровнена в одну аккуратную горизонтальную линию.
2. [x] **Отчёты и PDF (`ReportsScreen.kt`, `AgpPdfGenerator.kt`, `AgpSheetPreviewModal.kt`)**:
   - Формулировка «индекс риска» переименована в «GRI (риск гипо)» в приложении, превью и печатном PDF.
   - Временная шкала в PDF выровнена в одну линию без шахматного смещения.
3. [x] **Экран «Настройки» (`SettingsScreen.kt`)**:
   - В самом верху внедрена компактная карточка «Профиль пациента» с аватаром, именем, статусом (возраст, диагноз, стаж) и стрелкой перехода.
   - По тапу на карточку открывается модальное окно полного редактирования профиля (`PatientProfileEditDialog`).
   - Убрана длинная «простыня» полей ввода из сворачивающегося блока, экран стал чистым и аккуратным.
4. [x] **Контроль качества**:
   - Unit-тесты успешно пройдены (`testDebugUnitTest`).
   - Сборка APK и установка на устройство через ADB (`Success`).

---

## 12. Пол пациента, педиатрический ИМТ ВОЗ, ориентиры нормогликемии и кнопка Назад [Completed]
1. [x] **Профиль пациента и гендер (`UserSettings.kt`, `SettingsRepositoryImpl.kt`, `SettingsScreen.kt`)**:
   - Добавлено поле пола `gender` (Мужской ♂ / Женский ♀).
   - Аватар профиля: автоматическая генерация инициалов (напр., «ЕК») с гендерной цветовой палитрой (изумрудный для мужчин, пурпурный для женщин).
2. [x] **Педиатрический расчёт ИМТ по шкале ВОЗ (`BmiCategory`)**:
   - Для детей и подростков (2–17 лет): расчёт по международным перцентильным таблицам ВОЗ/CDC (с разделением по полу и возрасту).
   - Для взрослых (≥18 лет): стандартные пороги ВОЗ (18.5 / 25.0 / 30.0).
   - Цветовая индикация категории ИМТ (Дефицит, Норма, Избыточный вес, Ожирение) и поясняющая плашка.
3. [x] **Разделение целей TIR и TING (`TrendsScreen.kt`)**:
   - Строка распределения красится в зелёный цвет только при достижении клинической цели TIR ≥ 70%.
   - Снято ошибочное зелёное выделение с TING (так как официальная норма ADA относится только к TIR).
4. [x] **Энциклопедические подсказки нормы здорового человека (Нормогликемия)**:
   - В `FocusScreen.kt` и `TrendsScreen.kt` детализированы диалоги параметров (Mean, eA1c, Min/Max, SD, %CV, TIR, TING):
     - Норма натощак (3.3–5.5 ммоль/л), средний сахар здоровых людей (4.5–5.8 ммоль/л).
     - Постпрандиальные всплески у здоровых людей без диабета (кратковременно до 8.5–10.0 ммоль/л на 20–30 минут).
     - Ночные физиологические спады во сне (до 3.3–3.8 ммоль/л).
5. [x] **Кнопка «Назад» внизу бланка предпросмотра (`AgpSheetPreviewModal.kt`)**:
   - Добавлена удобная полноразмерная кнопка «Назад» в самый низ модального окна предпросмотра PDF.
6. [x] **Контроль качества**:
   - Unit-тесты успешно пройдены (`testDebugUnitTest`).
   - Сборка APK и установка на устройство через ADB (`Success`).

---

## 13. Фиксированная верхняя панель (Top Bar) на основных экранах [Completed]
1. [x] **`FocusScreen.kt` (Текущие сутки)**:
   - Вынесена строка шапки (кнопка ☰ Меню, заголовок «Текущие сутки», бейдж Стрика 🔥) из `LazyColumn` в постоянный верхний контейнер над скроллом.
2. [x] **`TrendsScreen.kt` (Аналитика)**:
   - Вынесена строка шапки (кнопка ☰ Меню, заголовок «Аналитика») из `LazyColumn` в постоянный верхний контейнер над скроллом.
3. [x] **`ReportsScreen.kt` (Медицинский отчёт)**:
   - Вынесена строка шапки (кнопка ☰ Меню, заголовок «Медицинский отчёт», подзаголовок) из `LazyColumn` в постоянный верхний контейнер над скроллом.
4. [x] **`SettingsScreen.kt` (Настройки)**:
   - Вынесена строка шапки (кнопка ← Назад, заголовок «Настройки», кнопка ❓ Справка) из `LazyColumn` в постоянный верхний контейнер над скроллом.
5. [x] **Тестирование и сборка**:
   - Прогон unit-тестов (`testDebugUnitTest`).
   - Сборка и установка APK на устройство через ADB (`Success`).

---

---

## 14. Локальный ежедневный автобэкап и умное восстановление [Completed]
1. [x] **Модель настроек и манифест (`UserSettings.kt`, `SettingsRepositoryImpl.kt`, `AndroidManifest.xml`)**:
   - Поля `isAutoBackupEnabled: Boolean = true`, `lastBackupTimestamp: Long = 0L`.
   - Разрешения `MANAGE_EXTERNAL_STORAGE`, `READ_EXTERNAL_STORAGE`, `WRITE_EXTERNAL_STORAGE`.
2. [x] **Менеджер автобэкапа (`AutoBackupManager.kt`)**:
   - Директория: `/sdcard/TIRUp/Backups/` (и фоллбэк при отсутствии спец-разрешения).
   - Транзакционная запись: `tirup_backup.json` -> `.bak` -> новый `tirup_backup.json` -> удаление `.bak`.
   - Самовосстановление: при повреждении или прерывании восстанавливает `.bak` без участия пользователя.
   - Метод чтения заголовка бэкапа (быстрый предпросмотр: пациент, кол-во точек, дата).
   - Методы полного бэкапа и восстановления (Settings + Room DB).
3. [x] **Периодический запуск бэкапа (`DexdripBroadcastReceiver.kt`, `TirupApplication.kt`)**:
   - Автоматический бэкап раз в сутки при накоплении данных и при изменении профиля пациента.
4. [x] **Диалог умного восстановления при первом запуске / переустановке (`MainActivity.kt`)**:
   - При старте до завершения онбординга: обнаружение бэкапа и предложение восстановить историю в один клик.
5. [x] **Тумблер автобэкапа в Настройках (`SettingsScreen.kt`, `SettingsViewModel.kt`)**:
   - Переключатель «Ежедневный автобэкап» с индикацией даты и времени последнего бэкапа.
6. [x] **Тестирование и сборка**:
   - Unit-тесты (`testDebugUnitTest`) пройдены успешно.
   - Сборка и установка APK через ADB, проверено сохранение всех 397 измерений и профиля пациента в JSON.

---

## 15. Трёхэшелонная система тревог (Предиктивные, Основные, Критические) [Completed]
1. [x] **Математический предиктор тренда (`GlucoseTrendPredictor.kt`)**:
   - Анализ скользящего окна из последних 10–15 точек.
   - Расчёт производной скорости изменения $dG/dt$ (ммоль/л/мин) методом взвешенной линейной регрессии ($R^2 > 0.85$).
   - Экстраполяция уровня глюкозы на 15 минут вперёд и вычисление вероятного времени выхода за границы целевого диапазона.
2. [x] **Программный медицинский звуковой синтезатор (`MedicalSoundPlayer.kt`)**:
   - Генерация чистых синусоидальных волн через `AudioTrack` со сглаживанием Hann (без системных наложений).
   - Уровень 1: мягкий двухтональный перезвон (587 ➔ 880 Гц).
   - Уровень 2: тройной отчётливый медицинский сигнал с паузой 1.5 секунды между бипами (~4 сек).
   - Уровень 3: 12-секундная серия тревожной сирены на потоке будильника `USAGE_ALARM` со стробоскопом вспышки.
3. [x] **Адаптивный клинический Снуз и глушение тревог (`GlucoseAlertManager.kt`, `AlertActionReceiver.kt`)**:
   - **Гипогликемия**: 15 минут Снуз после реакции. Предохранитель: если сахар падает ниже 2.8 или резко снижается, Снуз сбрасывается и сирена кричит снова. При отсутствии реакции — повтор каждые 5 минут.
   - **Гипергликемия**: 30 минут начального Снуза на действие болюса. Проверка на 30–45 минутах: если сахар падает — Снуз продлевается; если не падает — реэскалация сирены! При отсутствии реакции — повтор каждые 15 минут.
   - Кнопка «✓ Принято (Снять тревогу)» прямо в шторке Android.
   - Автоматическое снятие тревоги при разблокировке экрана (`isPhoneInActiveUse`) или открытии приложения (`MainActivity.onResume`).
4. [x] **Пользовательский интерфейс настроек (`SettingsScreen.kt`, `SettingsViewModel.kt`)**:
   - Карточка перенесена в **Основные настройки** (сразу под профилем/темой).
   - Свёрнута по умолчанию с **Главным Master-тумблером** включения/выключения всех тревог.
   - Цветовая дифференциация уровней: Синий (ActionBlue), Янтарный (ColorHigh), Тревожный красный (ColorVeryLow).
   - Кнопки мгновенного тестирования «▶️ Тест» для каждого уровня.
5. [x] **Интеграция в приёмник замеров (`DexdripBroadcastReceiver.kt`)**:
   - При каждом поступающем замере запуск анализа и выдача соответствующего уровня тревоги.
6. [x] **Тестирование и проверка сборки**:
   - Unit-тесты `GlucoseTrendPredictorTest` успешно пройдены.
   - Сборка и установка APK на реальное устройство через ADB.

---

## 16. Комплекс оптимизаций UI/UX, тревог и клинического анализа [Completed]
1. [x] **Фаза 1: Точное время в прогнозах + Календарный суточный TIR (с 00:00:00) + Компактный замер сахара**:
   - `GlucoseAlertManager.kt`: в предиктивных пушах форматировать точное расчётное время выхода (`в 16:42` вместо `~5 мин`).
   - `FocusViewModel.kt` & `TargetCompensatorCalculator.kt`: расчёт суточного TIR и статистики строго с 00:00:00 текущих суток.
   - `FocusScreen.kt`: убрать текст «последнее измерение», показывать таймер только если прошло >1 мин («3 мин назад»), добавить строку оперативного статуса.
2. [x] **Фаза 2: Тревога потери связи (>20 мин с экспоненциальным бэкоффом) + Мгновенное глушение аппаратными кнопками**:
   - `AlertSettings.kt`, `SettingsRepositoryImpl.kt`: параметры `isSignalLossEnabled` и интервал.
   - `GlucoseAlertManager.kt`: 4-я тревога потери данных с нарастающим интервалом (20 ➔ 40 ➔ 80 мин), метод мгновенного глушения звука без сброса снуза.
   - `MainActivity.kt`: перехват качельки громкости и кнопки питания для мгновенной остановки сирены.
3. [x] **Фаза 3: Справка по экспорту из xDrip + Экран «Аналитика и тренды»**:
   - `ReportsScreen.kt`: пиктограмма подсказки с подробной инструкцией экспорта из xDrip+ на месте корзины, когда файл не выбран.
   - `TrendsScreen.kt`: переименование экрана в «Аналитика и тренды», удаление блока компенсации, перенос паттернов наверх с индивидуальными кнопками скрытия (✕), тумблер [📊 График | 🔢 Параметры] в Амбулаторном профиле.
   - Уведомления об обнаруженных паттернах в шторку Android (не чаще 1 раза в сутки).
4. [x] **Фаза 4: Ультракомпактная нижняя панель навигации с авто-скрытием**:
   - Уменьшение высоты панели в 2 раза (44dp).
   - Плавное авто-скрытие панели при скролле вниз и показ при скролле вверх.

---

## 18. Редизайн «Справочника параметров CGM» в PDF и добавление «Руководства пользователя TIRUp» в Настройки [Completed]
1. [x] **Редизайн «Справочника параметров CGM» в PDF (`GuidebookPdfGenerator.kt`)**:
   - Верхний отступ увеличен до 44pt.
   - Заголовок: «TIRUp • Справочник параметров CGM» с официальным подзаголовком консенсуса ATTD / ADA.
   - Увеличена высота карточек (39–40pt) и межстрочный интервал (отсутствие слипания текста).
   - Лист А4 заполнен на 100% без пустот: 12 ключевых метрик разбиты по 4 секциям с цветными бейджами целевых ориентиров (Mean Glucose, eA1c/GMI, Min/Max, TIR, TING, TBR, TAR, %CV и SD, GVI и GRI, PGS, окно ночного сна, феномен «утренней зари» и постпрандиальные пики).
   - Аккуратный блок дисклеймера CGM гармонично расположен внизу страницы.
2. [x] **Очистка модального окна и карточки справочника (`ReportsScreen.kt`)**:
   - В верхней панели `ParametersGuidebookModal` удалены дублирующие пиктограммы скачивания, шаринга и закрытия, оставлен аккуратный заголовок с иконкой книги.
   - Заголовок карточки обновлен на «📖 Справочник параметров CGM» (подзаголовок: «Клинические нормы ATTD/ADA, смысл метрик и дисклеймер»).
3. [x] **Генератор руководства пользователя в PDF и интеграция в Настройки (`UserManualPdfGenerator.kt`, `SettingsViewModel.kt`, `HelpAndDisclaimerDialog.kt`)**:
   - Создан `UserManualPdfGenerator` для формирования чистого PDF-руководства пользователя (связка с xDrip+, обзор 3 ключевых экранов, 4 уровня тревог, снуз и глушение аппаратными кнопками, автобэкап в 23:59:59).
   - В `HelpAndDisclaimerDialog` добавлены информационные карточки по тревогам и автобэкапу, а также кнопка «📄 Распечатать руководство по TIRUp (PDF)».
   - В `SettingsViewModel` подключен метод печати/шеринга PDF через системный `FileProvider` и `Intent.ACTION_SEND`.
4. [x] **Тестирование и развёртывание**:
   - Unit-тесты и сборка `testDebugUnitTest assembleDebug` успешно пройдены.
   - Обновлённый APK установлен на устройство через ADB.

---

## 19. Синхронизация стрелок тренда, автоочистка уведомлений, защита критической тревоги гипо и интеграция IoB [Completed]
1. [x] **Синхронизация стрелок в предиктивных уведомлениях (`GlucoseAlertManager.kt`)**:
   - Стрелка тренда в уведомлениях рассчитывается строго из математической скорости изменения сахара (`↓↓`, `↓`, `↘` при падении; `↗`, `↑`, `↑↑` при росте). Исключены противоречия («сахар падает, стрелки вверх»).
2. [x] **Автоочистка уведомлений**:
   - Добавлен системный таймаут `.setTimeoutAfter(60 * 60 * 1000L)` для авто-удаления устаревших пушей из шторки Android через 60 минут.
   - При возврате сахара в целевой диапазон (TIR) неактуальные предиктивные и основные оповещения автоматически снимаются (`NotificationManager.cancel`).
3. [x] **Клиническая защита критической тревоги гипогликемии (`SettingsScreen.kt`, `AlertSettings.kt`)**:
   - При выключении тумблера критических тревог по умолчанию активируется безопасная временная пауза на 2 часа с таймером обратного отсчёта и авто-возобновлением.
   - Полное бессрочное отключение доступно только через диалог с обязательным осознанным чекбоксом «Я осознаю смертельный риск гипогликемической комы и беру ответственность на себя».
4. [x] **Мультимодальный главный блок и IoB/CoB (`FocusScreen.kt`, `DexdripBroadcastReceiver.kt`, `GlucoseReading.kt`, `Room`)**:
   - В `DexdripBroadcastReceiver` реализован парсинг активного инсулина (`iob`) и углеводов (`cob`) из broadcast-интентов (xDrip+, AndroidAPS, GlucoDataHandler, Nightscout).
   - Сущности `GlucoseReading` и Room DB дополнены полями `iob` и `cob` (версия базы данных повышена до 4).
   - На экране «Текущие сутки» убран статический заголовок «Глюкоза», карточка преобразована в мультимодальное окно статуса с аккуратными бейджами `💉 %.2f Ед IoB` и `🍞 %.0f г CoB`.
   - В `GlucoseAlertManager` внедрён учёт активного инсулина: ранняя предиктивная тревога при высоком IoB и сахаре на пороге снижения, а также продление гипер-снуза при активном болюсе для защиты от инсулинового наслоения (insulin stacking).
5. [x] **Тестирование и развёртывание**:
   - Все 26 модульных тестов пройдены успешно (`testDebugUnitTest`).
   - Собран APK и установлен на подключённое устройство через ADB (`Performing Streamed Install: Success`).

---

## 20. Исправление миграции Room, приём IoB на фолловере, защита вспышки, паттерн СТГ, PDF-вёрстка и единый блок 12 метрик [Completed]
1. [x] **Безопасная миграция Room DB без потери истории (`AppDatabase.kt`)**:
   - Реализован явный объект миграции `MIGRATION_3_4`: `ALTER TABLE glucose_readings ADD COLUMN iob REAL DEFAULT NULL; ALTER TABLE glucose_readings ADD COLUMN cob REAL DEFAULT NULL;`.
   - Зарегистрирован через `.addMigrations(MIGRATION_3_4)`. Обновления структуры базы данных теперь происходят бесшовно без потери истории замеров.
2. [x] **Приём IoB из xDrip+ на телефоне-фолловере (`AndroidManifest.xml`, `DexdripBroadcastReceiver.kt`)**:
   - Добавлен фильтр интента `com.eveningoutpost.dexdrip.watch.wearintegration.BROADCAST_SERVICE_SENDER`.
   - В `DexdripBroadcastReceiver` добавлен парсинг `predict.IOB` (число активного инсулина с локализованной запятой), `treatment.insulin`, `predict.BWP` (активные углеводы), `bg.valueMgdl`, `bg.timeStamp` и `bg.deltaName`.
   - Приложение на фолловере мгновенно получает и отображает IoB/CoB аналогично GlucoDataHandler.
3. [x] **Гарантированное отключение вспышки после тревоги (`GlucoseAlertManager.kt`)**:
   - В `triggerFlashlight` добавлен блок `finally { withContext(NonCancellable) { activeCameraId?.let { cameraManager.setTorchMode(it, false) } } }`.
   - Фонарик физически гарантированно отключается при отмене корутины, сбросе сирены пользователем или завершении цикла импульсов.
4. [x] **Защита критической тревоги гипо при отключении общего тумблера и HUD-хинт (`GlucoseAlertManager.kt`, `SettingsScreen.kt`)**:
   - При отключении главного переключателя тревог критическая тревога гипогликемии (<3.0 ммоль/л) переводится на 2-часовую паузу и автоматически возобновляет дежурство по истечении 2 часов.
   - По центру экрана на 3 секунды появляется анимированный плавающий HUD-баннер: *«Оповещения отключены. Критическая сирена (<3.0) на паузе 2 часа для вашей безопасности»*.
5. [x] **Паттерн ночных всплесков гормона роста (СТГ / соматотропина) (`PatternRecognitionEngine.kt`)**:
   - Внедрена детекция эпизодических ночных всплесков (23:00–03:00, 2–3 раза в неделю) для детей, подростков и лиц до 25 лет: $p90 \ge 9.5$ ммоль/л при спокойном вечере ($p50 \le 8.5$) и нормальной медиане первой половины ночи ($p50 \le 7.8$).
   - Выводится информационная карточка `🧬 Ночные всплески соматотропина / СТГ (23:00–03:00)` с клиническим комментарием.
6. [x] **Исправление вертикальных интервалов в PDF (`GuidebookPdfGenerator.kt`, `UserManualPdfGenerator.kt`)**:
   - Увеличен отступ сверху перед заголовком блока (`y += 11f`), отделяя заголовок от предыдущих карточек.
   - Уменьшен отступ снизу под заголовком (`y += 7.5f`), обеспечивая визуальное единство заголовка со своими карточками.
7. [x] **Локализация подписи и темы при отправке PDF (`ReportsViewModel.kt`)**:
   - Метод `dispatchShareIntent` теперь принимает динамические `subject` и `chooserTitle` с учётом языка (`RU` / `EN`).
   - Для справочника: *«TIRUp • Справочник параметров CGM»*, для отчёта: *«TIRUp • Амбулаторный гликемический профиль (AGP)»*.
   - Текст передаётся и в `EXTRA_TEXT`, автоматически заполняя подпись к документу в Telegram и WhatsApp.
8. [x] **Объединение 12 параметров на экране «Текущие сутки» в один монолитный блок (`FocusScreen.kt`)**:
   - Сетка из 12 показателей объединена в единый контейнер `BentoCard` с заголовком *«📊 Клинические параметры суток»*.
   - Устранён визуальный шум от 12 отдельных плавающих карточек, сохранена интерактивность и вызов подробных справочных модалок.
9. [x] **Тестирование и валидация**:
   - Компиляция `compileDebugKotlin` успешна.
   - Все 26 модульных тестов `testDebugUnitTest` успешно пройдены.

---

---

## 21. Точные суточные расчёты компенсатора, кнопки отчётов, разворачивание тревог и серый сахар при неактуальности [Completed]
1. [x] **Разворачивание списка тревог при выключенном тумблере (`SettingsScreen.kt`)**:
   - Снято ограничение `&& alerts.isAlertsMasterEnabled` при раскрытии списка тревог. Пользователь может в любой момент развернуть список и бессрочно отключить или настроить критическую сирену без необходимости повторно включать все тревоги.
   - Добавлен предупреждающий баннер при выключенном мастер-тумблере.
2. [x] **Стилизация кнопок в Справочнике, Превью бланка и Отчётах (`AgpSheetPreviewModal.kt`, `ReportsScreen.kt`)**:
   - В модальном окне Справочника и превью бланка AGP:
     - Кнопка «Назад» оформлена синей заливкой (`ActionBlue`), как в Настройках.
     - Кнопка «Сохранить» приведена к единому стилю с кнопкой «Поделиться» (`OutlinedButton` с акцентной окантовкой).
   - В карточках текущего и исторического отчётов ниже списка параметров кнопка «Сохранить PDF» избавлена от синего цвета и оформлена как нейтральная вторичная кнопка.
3. [x] **Блок компенсации цели: расчёт времени покрытия и защита от ложного «выполнено» (`TargetCompensatorCalculator.kt`, `FocusScreen.kt`, `GlucoseStatistics.kt`)**:
   - Добавлен точный расчёт фактического времени активного мониторинга на основе дельт времени между замерами (поддержка 1-минутных, 5-минутных и неравномерных интервалов).
   - На карточке и в детальном диалоге компенсатора отображается фактический бейдж: `📊 X точек за сегодня (Y ч Y мин мониторинга)`.
   - Внедрён клинический порог достоверности (≥6 часов = 360 минут данных): если данных меньше 6 часов, статус не рапортует преждевременно «Цель выполнена досрочно», а информирует о сборе суточных данных со статусом `INSUFFICIENT_DATA`.
   - При текущем выходе сахара за границы сохраняется высший клинический приоритет: предупреждение «Сахар вне диапазона. Вернитесь в норму...».
4. [x] **Серый цвет сахара и стрелки при отсутствии данных более 5 минут (`FocusScreen.kt`)**:
   - При отсутствии новых данных более 5 минут (`diffMinutes > 5`) крупная цифра сахара и стрелка динамики автоматически приглушаются нейтральным серым цветом (`onSurfaceVariant.copy(alpha = 0.55f)`).
   - Как только поступает свежий замер, значение моментально загорается соответствующим диапазону клиническим цветом.
5. [x] **Тестирование и валидация**:
   - Добавлен юнит-тест `testDailyCompensatorFewPointsDoesNotTriggerExceeding`.
   - Все 21 модульный тест `testDebugUnitTest` успешно пройдены.
   - Собран APK и установлен на смартфон через ADB.

---

## 22. Суточный интерактивный график (Pinch-Zoom), мотиватор стрика, сортировка 12 параметров и синхронизация тревог [Completed]
1. [x] **Суточный интерактивный график глюкозы вторым блоком на экране «Текущие сутки» (`DailyGlucoseChart.kt`, `FocusScreen.kt`)**:
   - Расположен вторым блоком сразу под карточкой сахара (`HeroGlucoseCard`).
   - Масштабирование по горизонтали жестом щипка двумя пальцами (pinch-to-zoom) от 2 до 24 часов вокруг центра жеста.
   - Плавная горизонтальная прокрутка (pan/drag) влево-вправо.
   - Целевой коридор TIR (3.9–10.0 ммоль/л), линия TING (7.8), сетка времени HH:00 и вертикальная отметка «Сейчас».
   - Инспекция точки по тапу (scrubber): показ точного сахара и времени замера.
   - Лимит выборки увеличен до 1440 точек (24 часа при частоте 1 раз в минуту).
2. [x] **Экран-мотиватор стрик-достижений (`StreakMotivatorDialog.kt`, `FocusScreen.kt`, `UserSettings.kt`)**:
   - Показ при росте стрика (от 2 дней подряд и выше).
   - Пульсирующая анимация огня 🔥, клинический мотивационный текст о защите микрососудов и органов-мишеней.
   - Запоминание `lastStreakCelebratedDays` в SharedPreferences, чтобы поздравление показывалось ненавязчиво только раз в сутки при увеличении серии.
3. [x] **Самостоятельная пользовательская сортировка 12 параметров (`MetricsOrderDialog.kt`, `FocusScreen.kt`, `ReportsScreen.kt`)**:
   - Модальный диалог со стрелками ▲/▼ для перестановки параметров и кнопкой сброса к эталонному клиническому порядку.
   - Кнопка настройки порядка ⚙️ (иконка Tune) добавлена в шапку блока параметров на экране «Текущие сутки» и в карточках отчётов «Медицинский отчёт».
   - Динамический рендеринг всех 12 показателей в сохранённом порядке пользователя (`userSettings.metricsOrder`).
4. [x] **Синхронизация тумблеров тревог и таймер обратного отсчёта критической сирены (`SettingsScreen.kt`)**:
   - При выключении главного тумблера тревог все дочерние тумблеры (Предиктивные, Основные, Критические, Потеря сигнала) синхронно переходят в визуально выключенное состояние (`checked = false`).
   - При активной паузе на тумблере критической тревоги отображается яркий бейдж обратного отсчёта: `⏳ X ч Y мин` (или `⏳ Y мин`).
   - В описании под тумблером выводится точное время авто-возобновления сирены (`⏳ Пауза: ещё 1 ч 59 мин (авто-возобновление в 20:40)`).
   - При повторном включении любого дочернего тумблера или снятии паузы критической сирены оповещения автоматически активируются.
5. [x] **Тестирование и развёртывание**:
   - Все 26 модульных тестов `testDebugUnitTest` успешно пройдены.
   - APK скомпилирован и установлен на реальное устройство через ADB.

---

## 23. Дорожная карта и следующие задачи (Roadmap)
- **Бонусный углублённый анализ при обработке исторических файлов гликемии с дополнительными данными (Калибровки глюкометром, Углеводы, Инсулин)**:
  - 1. **Адекватность калибровки сенсора**: сопоставление контрольных замеров глюкометром (fingerstick BG) с точками сенсора в том же интервале времени, расчёт разности (bias), калибровочного MARD и проверка задержки интерстициальной жидкости (5–12 мин). Предупреждения о неудачных калибровках при резком тренде.
  - 2. **Углеводный коэффициент (УК / Carb Ratio)**: сопоставление введённых болюсов и граммов углеводов/ХЕ для расчёта фактической потребности в инсулине на еду.
  - 3. **Фактор чувствительности к инсулину (ФЧИ / ISF)**: анализ «чистых» корректировочных болюсов на высокий сахар без еды (на сколько ммоль/л снижает 1 ед. активного инсулина).
- **IoB на телефоне-фолловере (Задача для совместного тестирования с пользователем)**:
  - Проанализировать сырые intent extras, поступающие от связки xDrip+ ➔ TIRUp при внесении дозы инсулина на мастере/фолловере.
- **Квартальные и ежегодные напоминания**:
  - Каждые 90 дней — напоминание о сверке расчётного eA1c с лабораторным анализом HbA1c.
  - 31 декабря в 23:59:59 — создание и архивация годового AGP-файла в PDF в папке TIRUp/Reports и старт нового года.
- **Режим «TIRUp+» для телефона-моста ребёнка**:
  - Ночные часы (DiaNight): мягкое свечение экрана ночью с крупным сахаром.
  - Белый список звонков (whiteCall): защита от спам-звонков, пропуск только номеров родителей.
  - Переключатель режима в настройках приложения.
## 24. Объединённая карточка графика и параметров, скрытие метрик, закрепление селектора, подсказки TIR и улучшение ночного профиля [Completed]
1. [x] **Объединение суточного графика и параметров с переключателем (`DailyGlucoseChart.kt`, `FocusScreen.kt`)**:
   - В шапке карточки суточного профиля размещён переключатель `[ 📊 График | 🔢 Параметры ]`.
   - В режиме параметров отображается адаптивная сетка клинических показателей с кнопкой перехода в настройки ⚙️.
   - Устранены дублирующие подписи заголовков для исключения тавтологии на экране «Текущие сутки» и «Аналитика».
2. [x] **Скрытие и отображение параметров (`UserSettings.kt`, `SettingsRepositoryImpl.kt`, `MetricsOrderDialog.kt`, `ReportsScreen.kt`)**:
   - Добавлено персистентное поле `hiddenMetrics: List<String>`.
   - В диалоге сортировки внедрены кнопки видимости (👁️ / 👁️‍🗨️), приглушение скрытых строк, счётчик активных метрик (`X / 12`) и сброс.
   - Скрытые параметры динамически исключаются на главном экране и в медицинских отчётах.
3. [x] **Тумблер критической тревоги на паузе (`SettingsScreen.kt`)**:
   - При общем выключении тревог критический тумблер остаётся включённым в статусе паузы с пиктограммой `⏸️` в бегунке и янтарной подсветкой.
   - Тап открывает диалог осознанного риска со снятием паузы или полным отключением.
4. [x] **Закреплённый селектор периодов на экране аналитики (`TrendsScreen.kt`)**:
   - `CompactPeriodSelector` перенесён в фиксированную верхнюю панель `Surface` под заголовком и не уходит за экран при скролле.
5. [x] **Кнопка «Сохранить PDF» в синем фирменном стиле (`ReportsScreen.kt`)**:
   - Серая окантовка заменена на `ActionBlue` с соответствующим цветом иконки и шрифта.
6. [x] **Движок подсказок для роста TIR (`TirAdvisorEngine.kt`, `TrendsScreen.kt`, `TirAdvisorEngineTest.kt`)**:
   - Приоритизированная генерация практических советов на 1–2 предложения:
     - 🛡️ Безопасность при TBR > 4% (правило 15 минут, купирование без переедания).
     - ⏱️ Пре-болюс при TAR > 25% (пауза 10–15 минут для прироста +5–8% TIR).
     - 🌅 Утренний подъем (05:00–09:00).
     - 📉 Снижение вариабельности при %CV > 36%.
     - 🍽️ Задержка белков/жиров на поздний ужин.
     - 🎯 Фокус на узком диапазоне TING при TIR ≥ 75%.
     - 🧬 Гормональные ночные всплески (СТГ) у детей и лиц до 25 лет.
   - Карточка `💡 Фокусы для роста TIR` на экране аналитики.
7. [x] **Улучшение ночного профиля (`GlucoseStatistics.kt`, `GlucoseMetricsCalculator.kt`, `FocusScreen.kt`)**:
   - Расчёт достаточности данных по реальному времени ночного сна (`nightDurationMinutes >= 45`) вместо минутного количества точек.
   - Формулировка «Недостаточно данных (<1 ч сна)».
   - Вывод точных диагностических вердиктов прямо на карточке главного экрана:
     - `Стабильный профиль: TIR 98%, SD 0.8`
     - `Риск ночных гипо: TBR 4.2%`
     - `Ночные подъёмы: TAR 28%, SD 2.1`
     - `Высокая вариабельность: SD 2.4`
     - `Всплеск сна (СТГ): max 10.4`
   - Поддержка распознавания импульсного выброса соматотропина (гормона роста) в первой половине ночи с пояснениями в диалоге.
8. [x] **Тестирование и развёртывание**:
   - Все unit-тесты (`testDebugUnitTest`) пройдены без ошибок.
   - Приложение собрано и установлено на устройство через ADB.

---

## 25. Дорожная карта: Виджеты рабочего стола с индикатором TIR и кнопкой «Ночник» (Roadmap)
> **Статус**: Запланировано в приоритете на следующий диалог.

### 1. Архитектура и стек:
- **Фреймворк**: Jetpack Glance (`androidx.glance:glance-appwidget`) — современный декларативный Compose для Android AppWidgets.
- **Фоновое обновление**: Автоматический вызов обновления виджета из `DexdripBroadcastReceiver` сразу при получении свежего замера (каждые 1–5 мин) без лишней нагрузки на аккумулятор.

### 2. Варианты размеров:
1. **Основной эргономичный виджет 4x1 («Полоса гликемии + Компенсатор TIR + Ночник»)**:
   - **Слева (Блок сахара)**: крупный сахар в цвете диапазона (серый при задержке >5 мин), стрелка тренда, дельта замера, время замера («2м назад»), бейдж активного инсулина IoB (`💉 X.X U`).
   - **По центру/справа (Блок компенсатора TIR)**: процент TIR за сегодня (`82%`), горизонтальный прогресс-бар с засечкой нормы 70%, суточная рекомендация в часах/минутах (`«В норме ещё 2 ч 15 мин»` / `«Вернитесь в норму: нужно 3 ч 40 мин»`).
   - **Справа (Кнопка быстрого запуска)**: иконка `[🌙]` для мгновенного перехода в ночной режим / ночник перед сном в одно касание.
   - **Интерактивность**: тап по сахару ➔ `FocusScreen`, тап по полосе TIR ➔ диалог компенсатора, тап по `[🌙]` ➔ ночник.

2. **Расширенный дашборд 4x2 («Суточный Bento-виджет»)**:
   - Верхняя строка: Сахар, тренд, дельта, время замера, индикатор стрика (`🔥 N дней`), активный инсулин IoB, кнопка `[🌙]`.
   - Центральный блок: мини-график (sparkline) за последние 3–4 часа с коридором TIR (3.9–10.0).
   - Нижний блок: детальный компенсатор TIR за сутки (`TIR: 78% (13 ч 20 мин / норма 16 ч 48 мин)`), прогресс-бар и статус достижимости цели.

3. **Компактный виджет 2x2 («Фокус»)**:
   - Сахар, стрелка, дельта, индикатор TIR, запас времени компенсатора.

---

## 26. Оптимизация экрана аналитики: удаление избыточного «Стабильного профиля» и персистентное закрытие подсказок с кулдауном 14 дней [Completed]

### 26.1. Доменный слой (PatternRecognitionEngine):
- [x] Удалить генерацию позитивного паттерна `stable_profile` («Стабильный профиль без паттернов декомпенсации»). Раздел «Обнаруженные паттерны» должен отображаться исключительно при наличии клинических аномалий (ночные гипогликемии, «утренняя заря», всплески гормона роста, постпрандиальные пики, высокая вариабельность %CV).
- [x] Обновить юнит-тесты в `PatternRecognitionEngineTest.kt` (`assertTrue(patterns.isEmpty())`).

### 26.2. Презентационный слой и персистентность (TrendsViewModel, TrendsScreen, MainActivity):
- [x] В `TrendsViewModel` добавить поддержку персистентного хранения закрытых карточек в `SharedPreferences` («tirup_trends_dismissed») с временным лагом 14 дней (`DISMISSAL_COOLDOWN_MS = 14L * 86_400_000L`).
- [x] Добавить методы `dismissPattern(id)` и `dismissInsight(id)` и выставить потоки `dismissedPatternIds` и `dismissedInsightIds`.
- [x] В `MainActivity.kt` передавать `applicationContext` в конструктор `TrendsViewModel`.
- [x] В `TrendsScreen.kt`:
  - Использовать `dismissedPatternIds` и `dismissedInsightIds` из ViewModel (состояние сохраняется при свайпах между страницами пейджера и перезапусках приложения).
  - Добавить индивидуальную кнопку закрытия `[✕]` для каждого совета в блоке «💡 Фокусы для роста TIR».
  - Автоматически скрывать весь блок, если все советы закрыты.

---

## 27. Мотивирующее пуш-уведомление «Последний шанс» для суточного TIR (Daily Compensator Alert) [Completed]

### 27.1. Доменный слой и настройки:
- [x] В `AlertSettings.kt` добавить параметры `isLastChanceAlertEnabled: Boolean = true` и `lastChanceBufferMinutes: Int = 60`.
- [x] В `SettingsRepositoryImpl.kt` реализовать сохранение и чтение флага `KEY_ALERT_LAST_CHANCE_ENABLED`.

### 27.2. Движок оповещений, фоновый приёмник и настройки:
- [x] В `GlucoseAlertManager.kt`:
  - Инициализировать канал `CHANNEL_COMPENSATOR` («Суточный компенсатор TIR») с высокой важностью.
  - Реализовать метод `checkLastChanceAlert(context, todayReadings, latestReading, settings)`:
    - Проверка времени (вечер, после 16:00, когда оставшееся время дня $\le 8$ ч).
    - Расчёт необходимого времени в норме и запаса на ошибку (`slackMinutes = remainingMinutesToday - neededMinutesToday`).
    - Условие триггера: `neededMinutesToday in 1..remainingMinutesToday`, `slackMinutes <= 60`, сахар вне целевого диапазона.
    - Анти-спам: сохранение даты последней отправки (`YYYY-MM-DD`) в SharedPreferences (не более 1 раза в сутки).
    - Формирование информативного пуша с точными цифрами сахара, остатка времени и допустимого запаса.
- [x] В `DexdripBroadcastReceiver.kt`:
  - Загрузка суточных точек (`getReadingsBetweenSync`) от начала текущих суток и вызов `checkLastChanceAlert`.
- [x] В `SettingsScreen.kt`:
  - Добавить переключатель «Уведомление „Последний шанс“ (суточный TIR)» в карточку «Уведомления и сигналы».

---

## 28. Виджеты рабочего стола Android (Homescreen Widgets: 4x1/5x1, 4x2/5x2, 2x2) [Completed]

### 28.1. Графические ресурсы, фоны и XML-разметка (Этап 1):
- [x] Создать `widget_background.xml`: стиль Frosted Glass (`#D9101622`), скругление углов `18.dp`, тонкая полупрозрачная окантовка `#26FFFFFF`.
- [x] Создать `widget_strip.xml` (Полоса 4x1 ↔ 5x1):
  - Крупный сахар, стрелка тренда, устойчивая 5-минутная дельта.
  - Время замера.
  - Полоса суточного компенсатора TIR с засечкой цели 70% и текстовой рекомендацией.
  - Бейджи `IoB` и `CoB` (свободно отображаются при растяжении до 5 колонок).
  - Кнопка перехода в режим «Ночник» `[🌙]`.
- [x] Создать `widget_dashboard.xml` (Bento-дашборд 4x2 ↔ 5x2):
  - Все элементы полосы + контейнер под мини-график sparkline за 3–4 часа + индикатор победного стрика (`🔥 N дн`).
- [x] Создать `widget_compact.xml` (Компактный 2x2):
  - Крупный сахар, стрелка, дельта, суточный TIR %, кнопка ночника.
- [x] Создать XML-метаданные виджетов (`widget_strip_info.xml`, `widget_dashboard_info.xml`, `widget_compact_info.xml`) с `resizeMode="horizontal|vertical"`.

### 28.2. Движок обновления, отрисовка графика и интеграция (Этап 2):
- [x] `TirupWidgetUpdater.kt`:
  - Расчёт клинической 5-минутной дельты (поиск замера в окне 4–7 мин назад) для поддержки минутных и 5-минутных интервалов.
  - Отрисовка сглаженного sparkline-графика за 3–4 часа через `Canvas -> Bitmap` с полосой коридора нормы TIR (3.9–10.0 ммоль/л).
  - Настройка кликабельности (клик по сахару ➔ открытие `MainActivity`, клик по `[🌙]` ➔ запуск ночника).
- [x] Реализация классов провайдеров (`TirupStripWidgetProvider`, `TirupDashboardWidgetProvider`, `TirupCompactWidgetProvider`).
- [x] Регистрация виджетов и ресиверов в `AndroidManifest.xml`.
- [x] Вызов `TirupWidgetUpdater.updateAllWidgets(context)` из `DexdripBroadcastReceiver` при получении каждого замера.

---

## 29. Адаптация главного блока сахара и алертов под минутные замеры (Libre 3 / Dexcom G7) [Completed]

### 29.1. Главный экран (HeroGlucoseCard в FocusScreen.kt):
- [x] Определение скорости тренда: заменить расчет по двум соседним минутным замерам на клиническое 5-минутное окно (поиск замера в интервале 3.5–7.5 мин назад) для сглаживания микрошума сенсора.
- [x] Вывод клинической 5-минутной дельты (например, `+0.3 ммоль/л за 5 мин` / `+6 мг/дл`) рядом со стрелкой тренда.
- [x] Расчет времени непрерывной нормы («✨ В норме последние N ч»): заменить умножение числа точек на 5 минут на расчёт реальной разницы между таймстемпами (`(latest.timestamp - oldest.timestamp) / 3600000.0`).

### 29.2. Движок алертов и предиктора (GlucoseAlertManager.kt, GlucoseTrendPredictor.kt):
- [x] Tier 2 (Main alert): для подтверждения выхода из диапазона учитывать реальную длительность ($\ge 15$ минут вне диапазона), защищая от ложных срабатываний при 5-минутных всплесках на минутных сенсорах.
- [x] Линейная регрессия в `GlucoseTrendPredictor`: переход на временное скользящее окно 30 минут, обеспечивающее высокую помехоустойчивость как для 1-минутных, так и для 5-минутных сенсоров.

---

## 30. Устранение ошибки загрузки виджетов и суточная привязка стрика нормы [Completed]

### 30.1. Исправление виджетов рабочего стола:
- [x] Замена неподдерживаемых тегов `<View>` на `<FrameLayout>` в `widget_strip.xml` и `widget_dashboard.xml` (устранение `Class not allowed to be inflated in RemoteViews: android.view.View`).
- [x] Перевод `ProgressBar` на нативные `progressTint` / `progressBackgroundTint` вместо кастомных drawables с clip, которые вызывали сбой инфлейта в лаунчере.
- [x] Добавление `goAsync()` во все три провайдера виджетов в `TirupWidgetProviders.kt` для гарантированного завершения асинхронного обновления до остановки ресивера.

### 30.2. Суточная синхронизация статуса нормы (FocusScreen.kt):
- [x] Ограничение подсчёта непрерывной нормы границей текущих суток (с 00:00).
- [x] Если сахар в норме с полуночи: статус «✨ В норме с начала суток (X.X ч)».
- [x] Если сахар выходил из нормы сегодня: статус «✨ В норме последние N мин» или «✨ В норме последние X.X ч».

---

## 31. Настройка запаса времени для пуша «Последний шанс» (1ч, 1.5ч, 2ч) [Completed]
- [x] В `AlertSettings.kt` и `SettingsRepositoryImpl.kt` значение запаса по умолчанию переведено на 90 минут (1.5 ч).
- [x] В `SettingsScreen.kt` под тумблером «Последний шанс» добавлен ряд из 3 компактных взаимоисключающих сегментных кнопок:
  - `1 ч` (60 мин) — бледно-красный индикатор (строгий режим);
  - `1.5 ч` (90 мин, по умолчанию) — мягкий бледно-зелёный (рекомендуемый баланс);
  - `2 ч` (120 мин) — насыщенный зелёный (комфортный режим).
- [x] Мгновенное сохранение выбранного порога в SharedPreferences и использование в `GlucoseAlertManager.checkLastChanceAlert`.




