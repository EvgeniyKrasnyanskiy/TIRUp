# Дорожная карта развития TIRUp (Roadmap)

## 1. Звуковое оформление тревог (Кастомные звуки / GDH)
- [x] **Поддержка тревог GDH и экстремальных звуковых профилей**:
  - Реализован математический PCM-синтез (44.1 кГц, 16-бит) без утяжеления APK аудиофайлами (0 байт прироста размера сборки).
  - **Супер-ГИПО (<3.0 ммоль/л)**: непрерывная 50-секундная сирена ГО / GDH с частотной модуляцией 450–850 Гц со второй гармоникой на максимальной громкости `USAGE_ALARM`.
  - **Супер-ГИПЕР (>13.9 ммоль/л)**: 16-секундный высокочастотный пульсирующий сигнал (1760/2349 Гц).
  - **Затяжные тревоги**: стандартный 12-секундный медицинский сигнал.
  - **Экран SOS фоловера**: переведён на 50-секундную сирену Супер-ГИПО со стробоскопом вспышки и вибро.
  - Добавлены кнопки предпрослушивания в диалоге критических порогов и в блоке тестирования настроек.
  - Подробное описание и клинические протоколы добавлены в PDF руководство пользователя (`UserManualPdfGenerator.kt`) и спецификацию `CRITICAL_ALERTS_SPEC.md`.

---

## 2. Ввод IoB, CoB, BG и заметок с передачей в xDrip+
- [ ] **Форма ввода лечения** в TIRUp (отдельная кнопка на главном экране или в FAB):
  - Поля: инсулин (ед.), углеводы (г.), BG вручную (ммоль/л), заметка (текст).
  - Сохранение локально в `TreatmentEntity`.
  - Отправка broadcast `com.eveningoutpost.dexdrip.NSEmulator.TREATMENT` для xDrip+.
  - Учёт в расчёте CoB и IoB.

---

## 3. Диагностика и устранение расхождения точек TIRUp vs xDrip
- [ ] **100% совпадение количества точек**:
  - У пользователя в xDrip 447 точек против 274 в TIRUp за ~8 часов (показатель 1-минутного CGM сенсора Libre/Dexcom, где накапливается ~60 точек в час).
  - `syncFromLocalXdrip` адаптирован под 1-минутный шаг с выборкой до 1440 точек за сутки через `/sgv.json`.
  - Сверка в конце суток: в 23:59 перед сменой даты запускать контрольную синхронизацию и верификацию целостности базы за прошедшие сутки.
  - Анализ логов на мастере и фоловере по тегу `DexdripReceiver`.

---

## 4. Эффект «дыхание» на фоне экрана (слева/справа)
- [x] **Расширить анимацию «дыхание»**: сейчас эффект только на рамке входящего сообщения.
  - Добавить мягкую фоновую пульсацию на левую и правую область экрана (за пределами карточки сообщения).
  - Требования: ненавязчиво (низкая амплитуда), заметно, не должно отвлекать от текста.
  - Возможная реализация: `animateFloatAsState` с `InfiniteTransition` на `alpha` фоновых `Box`-оверлеев по краям.

---

## 5. Зум суточного графика до 1 часа
- [x] **Добавить уровень масштабирования 1ч** на главном экране (сейчас минимум 2ч):
  - Добавить опцию «1ч» в список интервалов масштабирования.
  - Обновить расчёт диапазона оси X при выборе 1ч.

---

## 6. Энергоэффективный AOD режим (Always-On Display) для мониторинга сахара
- [ ] **Архитектура AOD внутри TIRUp для AMOLED/OLED без выгорания (Burn-In)**:
  1. **Управление экраном и активность поверх блокировки**:
     - Использование флагов `setShowWhenLocked(true)`, `setTurnScreenOn(true)` и `FLAG_KEEP_SCREEN_ON`.
  2. **Абсолютно черный фон (#000000) и микро-яркость**:
     - Фон `#000000` (физическое отключение 95%+ пикселей матрицы AMOLED).
     - Управление локальной яркостью окна: `layoutParams.screenBrightness = 0.01f` (1% яркости окна без изменения системной настройки).
     - Приглушенные тона текста сахара (темно-серый `#555555`, приглушенный изумрудный `#006600`).
  3. **Событийно-ориентированная логика под 1-минутный CGM**:
     - Экран спящего смартфона полностью гаснет/чернеет.
     - При поступлении нового сахара (каждую минуту или 5 минут):
       - Экран «просыпается» на минимальной яркости на 3-5 секунд.
       - Показывает свежее значение + стрелку тренда в случайно смещенной позиции.
       - Если сахар в норме — возвращается в 0% потребления до следующего замера.
       - Если сахар вышел в зону тревоги (аларм) — запускается импульсный цикл «2 сек показ / 3-5 сек пауза» для привлечения внимания.
  4. **Защита от выгорания матриц (Anti-Burn-In)**:
     - Генерация случайного смещения `Offset(±30..50px)` от центра при каждом показе.
     - Периодическая ротация зон отображения (сверху / по центру / снизу).
