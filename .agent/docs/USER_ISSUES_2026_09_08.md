# Задачи от пользователя (08.09.2026) + Аудит текстов

## Статус: Аудит выполнен 09.09.2026 (git pull + ручная проверка кода)

---

## A. Аудит текстов (утверждён пользователем)
- [x] A1–A19 — Все 19 пунктов аудита выполнены

---

## B. Пользовательские задачи (26 пунктов)

### Выполнено и подтверждено кодом
- [x] **B3.** Клик батарейки: "Данные получены X мин/ч/дн. назад" + "Отключить BLE-мост в доп. настройках" — FocusScreen.kt L264–286
- [x] **B4.** Настройки BLE: время данных не только в минутах — SettingsScreen.kt
- [x] **B5.** Паттерны: минимум 3 суток (stats.daysCount < 3) — PatternRecognitionEngine.kt L32
- [x] **B6.** Min/Max добавлен в предпросмотр и PDF — AgpSheetPreviewModal.kt, AgpPdfGenerator.kt
- [x] **B7.** Сообщение об ошибке при невалидном файле — ReportsViewModel.kt
- [x] **B8.** Удалены "предожирение" и "морбидное" — PatientClinicalInfoDialogs.kt
- [x] **B10.** Ползунок прозрачности: синий вместо зелёного — SettingsScreen.kt
- [x] **B11.** BLE-мост иконки: X, Bluetooth, радио — SettingsScreen.kt
- [x] **B12.** Убраны плашки Выкл/Вкл в "Экстренное SMS" — SettingsScreen.kt
- [x] **B14.** Ночной профиль: только часы, без минут — SettingsScreen.kt L1892–1920
- [x] **B15.** "Точный будильник" → "Автоматический бэкап в полночь"
- [x] **B16.** "Журнал оповещений" → "Журнал тревог"
- [x] **B17.** Счётчик лучшей серии "Лучшая серия: X дней" — StreakMotivatorDialog.kt L169–188
- [x] **B19.** Цвета иконок: язык→синий, единицы→фиолет, тема→ч/б, колокольчик→жёлтый
- [x] **B20.** Убран подтекст из переключателя "Метки болюса и еды"
- [x] **B21.** Адаптивный порог тревоги для 5-мин сенсоров (3 точки) — GlucoseAlertManager.kt L881–883
- [x] **B22.** Напоминания о замене инс.набора отключаются при терапии шприц-ручками — DeviceStatusReminderWorker.kt L55
- [x] **B24.** Стрик в уведомлении (приоритет 3, при streakDays > 0) — TirupWidgetUpdater.kt L1183–1188
- [x] **B25.** В подсказку BLE и батарейки добавлено "Отключить в доп. настройках" — FocusScreen.kt L226, L282, L285
- [x] **B26.** Иконка (i) у "Горизонт предиктивной тревоги" — SettingsScreen.kt

---

## НЕ ВЫПОЛНЕНО — требует реализации

### B1. Рамка вокруг иконок инс.набора/сенсора
Файл: DeviceStatusChips.kt
Проблема: Иконки увеличены (30.sp) — OK. Но Surface с рамкой остаётся крупной:
  padding(horizontal = 8.dp, vertical = 6.dp) + RoundedCornerShape(12.dp)
Задача: Уменьшить рамку по высоте — сделать компактной как у StreakBadge:
  padding(horizontal = 6.dp, vertical = 2.dp) + RoundedCornerShape(8.dp)

---

### B2. BLE хинт — убрать баннер, перенести хинт в верхнюю часть экрана
Файл: FocusScreen.kt
Проблема: Агент добавил постоянный текстовый баннер "BLE-мост включён: приёмник.
  Для отключения откройте Дополнительные настройки." (строки 203–234). Нужно убрать.
Задача:
  1. Удалить блок (строки 203–235):
     if (userSettings.bleBridgeSettings.role != BleBridgeRole.DISABLED) { item { Surface("BLE-мост включён...") } }
  2. Реализовать всплывающий хинт (overlay) в ВЕРХНЕЙ части экрана (~60dp от верха),
     который появляется ~3 сек при получении нового BLE-пакета и показывает:
     "TIRUp: 5.5, заряд батареи XX%, уровень сигнала"
     Логика: отслеживать lastPacketTimestamp в FocusViewModel через StateFlow<Long>,
     при изменении показывать overlay через LaunchedEffect + delay(3000).
     Box(Modifier.fillMaxSize()) + Surface с Alignment.TopCenter + padding(top=60.dp).

---

### B9. "Профиль пациента" → "Мой профиль"
Файл: SettingsScreen.kt
Проблема: В PatientProfileSummaryCard (L2956) и PatientProfileEditDialog (L3088)
  название "Профиль пациента" / "Patient Profile".
Задача: Заменить на "Мой профиль" / "My Profile" в обоих местах.
Примечание: Цвет текста subtitle (ИМТ, углеводы) уже исправлен (onSurfaceVariant при hasName=true).
  При незаполненном профиле "Нажмите для заполнения..." остаётся синим — это intentional call-to-action.

---

### B13. "Напоминания об устройствах" — нет рамки/фрейма
Файл: SettingsScreen.kt
Проблема: Блок L1790–1824 обёрнут в обычный Row без BentoCard.
  Визуально отличается от всех остальных переключателей.
Задача: Обернуть блок в BentoCard(modifier = Modifier.fillMaxWidth()) по аналогии
  с блоком "Уведомление на экране блокировки" L1925+.

---

### B18. IoB и CoB не отображаются на графике
Файл: DailyGlucoseChart.kt
Проблема: В Canvas-рендеринге нет линий/кривых IoB и CoB.
  IoB/CoB есть только: badge в HeroCard (при iob>0) и тап-деталях на точке (L462–469).
Задача: Добавить вторичные линии на график:
  - IoB — синяя линия (пунктир), правая ось в единицах Ед
  - CoB — оранжевая линия, правая ось в г
  - Отображать только при наличии данных (iob!=null || cob!=null в readings)
ВАЖНО: Перед реализацией уточнить у пользователя желаемый вид (линия/заливка/точки).

---

### B23. Автоудаление устаревших уведомлений из шторки
Файлы: StalenessAlarmReceiver.kt, GlucoseAlertManager.kt
Проблема: StalenessAlarmReceiver только обновляет виджеты/постоянное уведомление,
  но НЕ отменяет устаревшие предиктивные/тревожные уведомления.
Задача:
  - Отменять предиктивные уведомления через настраиваемый интервал (1–12 ч)
  - Отменять тревоги гипо/гипер, если сахар вернулся в норму через X мин
  - По желанию: добавить настройку времени автоудаления в UserSettings

---

## Итого (актуально после аудита 09.09.2026)
- **Выполнено и подтверждено:** A1–A19 (19/19), B3, B4, B5, B6, B7, B8, B10, B11, B12, B14, B15, B16, B17, B19, B20, B21, B22, B24, B25, B26 (21/26)
- **Осталось:** B1, B2, B9, B13, B18, B23 (6/26)

## Приоритет реализации
1. B2 — убрать баннер + хинт (UX-регрессия)
2. B13 — добавить рамку (1-строчное изменение)
3. B1 — уменьшить рамку иконок
4. B9 — переименование "Мой профиль"
5. B23 — автоудаление уведомлений
6. B18 — IoB/CoB на графике (требует обсуждения)
