# [Выполнено] Роли Мастер/Фоловер, Heads-Up оверлей и улучшения тестирования

## Коммиты: 4c75799, 6c934c6, 387d401, 3fda3f7, b7104ed, 3e352e8, 774f55e

### Что сделано:
1. **Роль «Мастер» / «Фоловер»** — карточка выбора роли (Bento-стиль, 👑 / 👁️) добавлена прямо под «Мой профиль» в настройках; старые скрытые checkbox в разделе SMS удалены.
2. **Разделение теста тревог по роли** — для Мастера тест запускает `PatientCriticalHypoActivity`, для Фоловера — `CaregiverSosActivity`.
3. **Переименование в Мастер/Фоловер** — обновлены `BleBridgeDialogs.kt`, `AlertThresholdDialogs.kt`, `PatientCriticalHypoActivity.kt`.
4. **Heads-Up оверлей** — `HeadsUpMessageActivity.kt` с пульсирующей каймой, quick-reply (лимит 70 символов), wakelock. Зарегистрирован в манифесте. Интегрирован в `SmsQueryReceiver.kt`.
5. **Бесшумная телеметрия** — автозапросы сахара (`?`, `сахар`, `bg` и т.д.) обрабатываются полностью в фоне без пробуждения экрана.
6. **5-секундный countdown** для кнопок «SOS-экран фоловера» и «Важное сообщение» — аналогично кнопке «Экран спасения». Добавлены `startHeadsUpTestCountdown()` и `cancelHeadsUpTest()` в ViewModel.
7. **Подсказка о разрешениях** — при нажатии кнопок теста если нет overlay-разрешения — toast с инструкцией. Общая подсказка под кнопками.
8. **Каналы уведомлений на русский** — «Device Reminders» → «Напоминания об устройствах», «HbA1c Lab Reminders» → «Напоминания об анализах (HbA1c)».
9. **Руководство пользователя** — раздел 4.2 расширен: конкретные пути к разрешениям для полноэкранных уведомлений и всплывающих окон в фоне.

---

# План реализации: UX, телеметрия, HbA1c, янтарный стрик и Quick Glance HUD [Выполнено]


## 1. Выполненные доработки

1. [Выполнено] **Исправление пробивки экрана блокировки для экрана спасения** (`GlucoseAlertManager.kt`, `PatientCriticalHypoActivity.kt`):
   - Добавлен захват `WakeLock` с `PowerManager.ACQUIRE_CAUSES_WAKEUP | PowerManager.ON_AFTER_RELEASE`.
   - Добавлена публикация полноэкранного `NotificationCompat.Builder.setFullScreenIntent` с категорией `CATEGORY_ALARM`.
   - Добавлены флаги окна `FLAG_SHOW_WHEN_LOCKED`, `FLAG_TURN_SCREEN_ON`, `FLAG_KEEP_SCREEN_ON` для 100% пробивки на всех вендорах (MIUI, OneUI и др.).

2. [Выполнено] **Скрытый блок «Тестирование систем» и тестовое SMS** (`SettingsScreen.kt`):
   - Кнопка «Отправить тестовое SMS» перенесена в блок «Экстренное SMS».
   - Блок «Тестирование систем» скрыт по умолчанию и активируется по быстрому 5-кратному тапу по номеру версии внизу экрана с автоскрытием через 5 минут.
   - Дефолты обновлены: задержка экстренного SMS — 3 мин, буфер «Последнего шанса» — 120 мин (2 ч).
   - Устранены дублирующие тосты BLE, длительность теста дальности сокращена до 5 секунд.

3. [Выполнено] **Форматирование отрицательных счетчиков устройств** (`DeviceStatus.kt`):
   - До 24 часов: `-Xч`
   - От 1 до 30 дней: `-Xд`
   - Более 30 дней: `-Xм`

4. [Выполнено] **Цветовая индикация HbA1c** (`ReportsScreen.kt`, `SettingsScreen.kt`):
   - На экране отчётов рамка, иконка и текст кнопки окрашиваются по свежести анализа (<30д — `ColorTight`, 30-360д — `PrimaryEmerald`, >360д — `ColorHigh`).
   - Значение HbA1c окрашивается по 4-уровневой медицинской шкале (<6.1% — норма, 6.1-7.0% — целевой, 7.0-8.0% — субкомпенсация, >8.0% — красный).
   - В журнале HbA1c список анализов также выводится с цветными бейджами нормы.

5. [Выполнено] **Автозакрытие и архив клинических паттернов** (`TrendsViewModel.kt`, `TrendsScreen.kt`):
   - Паттерны `ALERT` закрываются через 48 часов, остальные через 24 часа; динамически исчезают при нормализации показателей.
   - Добавлен раскрывающийся архив скрытых паттернов с возможностью восстановления.

6. [Выполнено] **Автоскрытие нижней навигации и пробуждение по тапу** (`MainActivity.kt`):
   - Скрытие через 2.2 секунды неактивности.
   - Моментальное пробуждение при первом касании экрана в любом месте (`awaitFirstDown(requireUnconsumed = false)`).

7. [Выполнено] **Янтарный бейдж стрика** (`Components.kt`):
   - При `streakDays > 0` зелёный цвет заменён на тёплый янтарный (**Amber** `#F59E0B`): янтарная подложка, рамка и цифра стрика (`#FBBF24` / `#D97706`).

8. [Выполнено] **Quick Glance HUD по долгому тапу** (`MainActivity.kt`):
   - Задержка удержания центральной кнопки снижена до **0.33 с** (`delay(330L)`).
   - Центрированная карточка 60% высоты экрана.
   - Сахар увеличен до **`108sp`**, стрелка до **`78sp`**, единица `ммоль/л` (`24sp`) вынесена отдельной строкой строго по центру под сахаром.
   - Вёрстка карточек Инсулина и Углеводов переведена в вертикальную: заголовки `17sp`, значения **`34sp`** (`FontWeight.Black`), исключён перенос букв в столбик.
   - Телеметрия: сенсор `⏱️ Сенсор: 21д` (`19sp`), батарея и сигнал `🔋 25%  📶 -91 dBm` (`19sp`) в одну ровную строку без наслоений.

9. [Выполнено] **Исчерпывающее 6-страничное руководство пользователя (PDF)** (`UserManualPdfGenerator.kt`, `ROADMAP.md`):
   - Генератор переведён на классическую книжную типографику с главами, разделами (§1.1–§6.6), связными параграфами, маркированными списками и акцентными выносными блоками безопасности (Callouts: INFO, TIP, WARNING, CRITICAL).
   - Включены все 6 ключевых глав:
     1) Архитектура Zero-Cloud, интеграция с источниками (xDrip+, GDH, Juggluco) и Семейный BLE-мост (Long Range LE Coded PHY, 16-байтный протокол, Family PIN).
     2) Энергосбережение Android (Doze Mode) и пошаговые инструкции для OEM (Samsung OneUI, Xiaomi MIUI/HyperOS, BBK Realme/Oppo/OnePlus).
     3) Четырёхуровневая система безопасности (Tier 1–4), предиктивный тренд на 25 минут, Экран спасения и протокол защиты от комы (<2.8 ммоль/л).
     4) Экстренные SOS SMS с геолокацией, двусторонняя оффлайн-телеметрия, Режим опекуна (сирена будильника опекуна) и Dev-блок тестирования.
     5) Клиническая аналитика AGP (12 параметров), распознавание скрытых паттернов, журнал HbA1c и суточный компенсатор TIR с точкой невозврата.
     6) Quick Glance HUD (108sp), виджеты рабочего стола, учёт расходников с отрицательными счётчиками, Zero-Lag база данных, резервные копии и медицинский отказ от ответственности.
   - Полная двуязычная поддержка (RU / EN).

10. [Выполнено] **Ревью кода и вёрстки генератора PDF-руководства пользователя** (`UserManualPdfGenerator.kt`, `.agent/docs/analysis_results.md`):
    - Исправлен мёртвый код и расчёт отступов при переносе строк в `drawBulletPoint`.
    - Внедрён динамический расчёт высоты выносных блоков `drawCallout` от фактического числа строк текста, полностью исключено обрезание текста на всех 6 страницах.
    - Заголовки буллетов выделены полужирным начертанием `paintBold`.
    - Добавлен блок содержания (Table of Contents) на Стр. 1.
    - Добавлен блок «Быстрый старт за 3 шага» на Стр. 1.
    - Добавлены чеклисты настройки фоновой работы для вендоров Huawei / Honor (EMUI / MagicOS) и Google Pixel на Стр. 2.
    - Версия приложения теперь извлекается динамически через `packageManager.getPackageInfo` вместо фиксированной константы.
    - Бегущий колонтитул локализован для русской версии документа.

11. [Выполнено] **Оптимизация вёрстки руководства пользователя: 2 главы на страницу и устранение наслоений текста** (`UserManualPdfGenerator.kt`, `scratch_manual.py`):
    - Удалено избыточное слово «ПОЛНОЕ» из титульного блока документа (`TIRUp • РУКОВОДСТВО ПОЛЬЗОВАТЕЛЯ`).
    - Устранено наложение строк и заголовков буллетов: расчёт базовой линии `y` переведён на строгий шаг `lineHeight` (9.6f) с межэлементными отступами (3.0–4.0f).
    - Плотная вёрстка «2 главы на страницу» (3 страницы A4 вместо 6) без пустот в нижней половине страниц:
      - Стр. 1: Введение, Содержание, Быстрый старт + Глава 1 (Связь, источники, BLE-мост, Long Range) + Глава 2 (Энергосбережение, чеклисты Samsung, Xiaomi, BBK, Huawei, Pixel, WakeLock).
      - Стр. 2: Глава 3 (Система тревог Tier 1–4, тренд на 25 мин, Экран спасения, Coma Guard, Правило 15) + Глава 4 (Экстренные SOS SMS с GPS, оффлайн-телеметрия, Режим опекуна, права).
      - Стр. 3: Глава 5 (Клинический AGP, 12 параметров, паттерны, журнал HbA1c, компенсатор) + Глава 6 (HUD 108sp, виджеты, расходники, Zero-Lag база, бэкапы, отказ от ответственности).
    - Устранены пересечения текста в верхнем и нижнем колонтитулах.
    - Разработан автономный скрач-скрипт (`scratch_manual.py`) и сгенерирован тестовый файл `TIRUp_User_Manual_Test.pdf` без необходимости долгой сборки APK.

12. [Выполнено] **Увеличение отступа между главами в 3 раза, устранение разрыва в буллетах и увеличение шрифта** (`UserManualPdfGenerator.kt`, `.agent/scripts/scratch_manual.py`):
    - Отступ между главами на каждой странице (между Гл. 1 и Гл. 2, Гл. 3 и Гл. 4, Гл. 5 и Гл. 6) увеличен в 3 раза (с 8f до 22–24f) для визуального разделения разделов.
    - Устранён гигантский пробельный разрыв между полужирными заголовками буллетов и их описанием: ширина заголовка в скрипте генерации измерялась по системному экрану (96 DPI = 213px) вместо PDF-холста (72 DPI = 157px). Замер переведён на точные координаты холста `painter.fontMetrics()`.
    - Шрифт основного текста увеличен с 7.0pt до 8.0pt (межстрочный интервал `lineHeight = 10.4f`), заголовки разделов до 8.0pt bold, заголовки глав до 10.0pt bold, титульный заголовок до 12.0pt bold — текст стал крупным, чётким и легко читаемым при сохранении формата «2 главы на страницу» (3 страницы A4).
    - Изменения синхронизированы в `UserManualPdfGenerator.kt` и проверены через `.\gradlew compileDebugKotlin` без долгой сборки APK.
    - Сгенерирован актуальный тестовый PDF: `TIRUp_User_Manual_Test.pdf`.

13. [Выполнено] **Модуляризация экрана настроек SettingsScreen.kt** (`SettingsScreen.kt`, `Hba1cHistoryDialog.kt`, `YearEndDigestDialog.kt`, `PatientProfileDialogs.kt`, `BleBridgeDialogs.kt`, `AlertThresholdDialogs.kt`):
    - Создан пакет `com.tirup.app.presentation.settings.dialogs`.
    - Вынесены крупные изолированные диалоги и секции:
      - `Hba1cHistoryDialog.kt`: журнал анализов гликированного гемоглобина, валидация, калькуляторы и экспорт PDF.
      - `YearEndDigestDialog.kt`: праздничный дайджест со сводной аналитикой, анимированными графиками и PDF-сертификатом.
      - `PatientProfileDialogs.kt`: карточка профиля пациента (`PatientProfileSummaryCard`), диалог редактирования (`PatientProfileEditDialog`) и кастомные выпадающие селекторы.
      - `BleBridgeDialogs.kt`: справка по локальному BLE-мосту (`BleBridgeHelpDialog`), диалог смены PIN-кода семьи (`BleFamilyPinDialog`), подтверждение режима повышенной дальности (`BleLongRangeConfirmDialog`) и тест дальности (`BleRangeHelpDialog`).
      - `AlertThresholdDialogs.kt`: настройка порогов основных тревог (`MainThresholdDialog`), критических сирен (`CriticalThresholdDialog`), предиктивного горизонта (`PredictiveHorizonDialog`), справки (`PredictiveInfoDialog`) и протокола защиты от комы (`CriticalHypoSafetyDialog`).
    - Размер `SettingsScreen.kt` сокращён более чем на 3080 строк кода (с ~6900 до ~3800 строк).
    - Код проверен пошаговой компиляцией через `compileDebugKotlin` с нулевыми ошибками.

---

# План реализации: Экспериментальный режим повышенной дальности BLE-моста (Long Range / LE Coded PHY) [v2.2.0]

## 1. Задачи для реализации

1. [Выполнено] **Доменная модель и вещатель (Broadcaster)** (`BleBridgeSettings.kt`, `SettingsRepositoryImpl.kt`, `AutoBackupManager.kt`, `BleBroadcaster.kt`):
   - Добавлено свойство `useLongRange: Boolean = false` в `BleBridgeSettings`.
   - Настроено сохранение и загрузка `KEY_BLE_BRIDGE_USE_LONG_RANGE` в SharedPreferences.
   - Сериализация в JSON бэкапа и безопасная десериализация старых архивов (с дефолтом `false`).
   - Вещатель `BleBroadcaster`: проверка аппаратной поддержки (`isLeCodedPhySupported && isLeExtendedAdvertisingSupported`), запуск `AdvertisingSetParameters` с `PHY_LE_CODED`, fail-safe fallback на классический `startAdvertising` при ошибке `ADVERTISE_FAILED_FEATURE_UNSUPPORTED`.
   - 100% сохранение структуры 16-байтного пакета `BlePacketCodec` без изменения бит PIN-кода для обратной совместимости.

2. [Выполнено] **Всеядный сканер на стороне приёмника (Observer)** (`BleObserverManager.kt`):
   - Проверка `isLeExtendedAdvertisingSupported` при старте сканера.
   - Настройка `ScanSettings.setLegacy(false).setPhy(PHY_LE_ALL_SUPPORTED)` для параллельного приёма Legacy 1M и Coded PHY.
   - Fail-safe fallback: при `SCAN_FAILED_FEATURE_UNSUPPORTED` прозрачный перезапуск сканера с `setLegacy(true)`.
   - Детекция `ScanResult.primaryPhy == BluetoothDevice.PHY_LE_CODED` и передача состояния в StateFlow (`lastPacketWasLongRange`, `isLongRangeScanActive`).

3. [Выполнено] **Пользовательский интерфейс и диагностика (UI / UX)** (`SettingsScreen.kt`, `FocusScreen.kt`):
   - Тумблер в настройках моста с аппаратной блокировкой и поясняющим диалогом подтверждения.
   - Кнопка Test Ping (30 сек) с визуальным отсчётом таймера.
   - Бейдж `BleBridgeBadge`: `📡 LR` на вещателе и `📻 Dual / Standard` на приёмнике.
   - Мгновенный отклик бейджа на тестовый пинг для наглядного подтверждения приёма.
   - Адресная диагностика тишины (>6 мин) на устаревшем сканере с рекомендацией переключить вещатель в Legacy.

4. [Выполнено] **Обновление документации и руководств** (`UserManualPdfGenerator.kt`, `README.md`, `ROADMAP.md`):
   - Актуализировано 2-страничное руководство пользователя PDF.
   - Дополнен раздел 14 README.md и дорожная карта.

---

# План реализации: Линия прогноза на суточном графике (25 минут)

## 1. Задачи для реализации

1. [Выполнено] **Математика генерации прогнозных точек на 25 минут** (`GlucoseTrendPredictor.kt`):
   - Добавлена модель `ForecastPoint(timeMillis, glucoseMmol, deltaMinutes)`.
   - Добавлена функция `generateForecastPoints(readings: List<GlucoseReading>, horizonMinutes: Int = 25, stepMinutes: Int = 5): List<ForecastPoint>`.
   - Проверка свежести данных (не старше 15 минут), минимальное кол-во точек (5 шт).
   - Линейная регрессия за последние 30 минут с экспоненциальным затуханием наклона (damping factor от 1.0 до 0.4) и ограничением значений 1.5–25.0 ммоль/л.

2. [Выполнено] **Настройка в UserSettings и SettingsScreen** (`UserSettings.kt`, `SettingsRepositoryImpl.kt`, `SettingsViewModel.kt`, `SettingsScreen.kt`):
   - Добавлено поле `showPredictionOnChart: Boolean = true` в `UserSettings`.
   - Реализовано сохранение и загрузка в `SettingsRepositoryImpl` и `SettingsViewModel`.
   - В `SettingsScreen` добавлен тумблер: «Линия прогноза на графике (25 мин)».
   - Проброшен флаг через `FocusScreen` в `DailyGlucoseChart`.

3. [Выполнено] **Отрисовка прогноза на суточном графике** (`DailyGlucoseChart.kt`):
   - При `showPrediction = true` рассчитываются прогнозные точки от последнего замера.
   - Отрисовываются светящиеся фиолетовые точки (`#A855F7`) на отметках `+5м`, `+10м`, `+15м`, `+20м`, `+25м`.
   - Точки соединены пунктирной фиолетовой линией (`DashPathEffect(floatArrayOf(8f, 8f), 0f)`).
   - Реализован тап по точкам прогноза с отображением инспектор-баннера: `🔮 Прогноз HH:mm (+Xм): ~X.X ммоль/л`.

4. [Выполнено] **Обновление документации** (`UserManualPdfGenerator.kt`, `README.md`):
   - В `UserManualPdfGenerator.kt` добавлено описание фиолетовой пунктирной линии прогноза на 25 минут.
   - В `README.md` добавлен раздел 5 с описанием фичи, математики затухания и визуализации.

---

# Выполненные задачи из Roadmap

# План реализации: Линия прогноза на суточном графике (25 минут)

## 1. Задачи для реализации

1. [Выполнено] **Математика генерации прогнозных точек на 25 минут** (`GlucoseTrendPredictor.kt`):
   - Добавлена модель `ForecastPoint(timeMillis, glucoseMmol, deltaMinutes)`.
   - Добавлена функция `generateForecastPoints(readings: List<GlucoseReading>, horizonMinutes: Int = 25, stepMinutes: Int = 5): List<ForecastPoint>`.
   - Проверка свежести данных (не старше 15 минут), минимальное кол-во точек (5 шт).
   - Линейная регрессия за последние 30 минут с экспоненциальным затуханием наклона (damping factor от 1.0 до 0.4) и ограничением значений 1.5–25.0 ммоль/л.

2. [Выполнено] **Настройка в UserSettings и SettingsScreen** (`UserSettings.kt`, `SettingsRepositoryImpl.kt`, `SettingsViewModel.kt`, `SettingsScreen.kt`):
   - Добавлено поле `showPredictionOnChart: Boolean = true` в `UserSettings`.
   - Реализовано сохранение и загрузка в `SettingsRepositoryImpl` и `SettingsViewModel`.
   - В `SettingsScreen` добавлен тумблер: «Линия прогноза на графике (25 мин)».
   - Проброшен флаг через `FocusScreen` в `DailyGlucoseChart`.

3. [Выполнено] **Отрисовка прогноза на суточном графике** (`DailyGlucoseChart.kt`):
   - При `showPrediction = true` рассчитываются прогнозные точки от последнего замера.
   - Отрисовываются светящиеся фиолетовые точки (`#A855F7`) на отметках `+5м`, `+10м`, `+15м`, `+20м`, `+25м`.
   - Точки соединены пунктирной фиолетовой линией (`DashPathEffect(floatArrayOf(8f, 8f), 0f)`).
   - Реализован тап по точкам прогноза с отображением инспектор-баннера: `🔮 Прогноз HH:mm (+Xм): ~X.X ммоль/л`.

4. [Выполнено] **Обновление документации** (`UserManualPdfGenerator.kt`, `README.md`):
   - В `UserManualPdfGenerator.kt` добавлено описание фиолетовой пунктирной линии прогноза на 25 минут.
   - В `README.md` добавлен раздел 5 с описанием фичи, математики затухания и визуализации.

---

# Выполненные задачи из Roadmap

### [Completed] 1. Гарантированная сохранность локальной БД и перенос данных на новый телефон (Zero-Data-Loss & Migration)
- **Гарантированная сохранность локальной БД (Zero-Data-Loss Architecture)**:
  - Полная изоляция истории замеров (`GlucoseReadingEntity`) и меток (`TreatmentEntity`) внутри базы данных Room приложения TIRUp, независимая от сбоев, очистки кэша или переустановок xDrip+.
  - Раздельное хранение данных в незашифрованном виде: замеры сахара в CSV (`tirup_readings.csv`), отметки терапии в CSV (`tirup_treatments.csv`), настройки и профиль в JSON (`tirup_settings.json`), плюс обратная совместимость со старым форматом `tirup_backup.json`.
  - Автоматический тихий ежедневный фоновый снапшот в общедоступную системную папку `Documents/TIRUp/Backups/` (не удаляется при удалении или переустановке приложения Android).
  - 100% автономность и конфиденциальность: отсутствие сетевых обращений, авторизаций и передачи персональных медицинских данных во внешние облачные сервисы.
- **Мастер переноса на новый смартфон без интернета (Direct Device-to-Device Sync)**:
  - Экспорт и импорт полного профиля: все локальные настройки, пороги тревог, профили сна, параметры сенсоров, калибровки, замеры, болюсы, углеводы и счетчики расходников в едином архиве `tirup_backup_*.zip`.
  - Мгновенный перенос всей накопленной истории и настроек при смене смартфона за 10–15 секунд через системный диалог «Поделиться» (Quick Share, Bluetooth, Telegram) либо выбор файла через системный проводник.
  - Предпросмотр содержимого бэкапа перед восстановлением с подтверждением в модальном окне.

---

### [Completed] 2. Квартальный рубеж HbA1c, журнал анализов, годовая ротация бэкапов и Семейный BLE-мост (v2.1.0)
- **Плашка и Журнал лабораторного HbA1c в карточке «Мой профиль»**:
  - Расположение: кликабельный медицинский бейдж `HbA1c: X.X%` (или `HbA1c: —`) в `PatientProfileSummaryCard` настроек.
  - Диалог `Hba1cHistoryDialog`: ввод даты, значения (%), лаборатории (опционально) и заметки; сопоставление с 90-дневным GMI сенсора и TIR.
  - Экспорт 1-страничной официальной выписки динамики HbA1c в PDF (`Hba1cReportPdfGenerator.kt`) с интерактивным снекбаром «Открыть».
  - Автоматическое дублирование последнего показателя HbA1c в основной AGP PDF-отчёт.
- **Квартальное напоминание о контроле HbA1c (раз в 90 дней)**:
  - Фоновый воркер `Hba1cReminderWorker.kt`: отсчёт 90 дней от последней точки БД или предыдущего анализа.
  - Интеллектуальная защита от спама: уведомление показывается не чаще 2 раз за цикл с шагом 14 дней.
  - Действие «Пропустить (+90д)» прямо в пуше: продлевает таймер для пользователей, сознательно не сдающих анализ в текущем квартале.
  - Внесение свежего анализа автоматически перезапускает 90-дневный цикл.
- **Новогодний диа-дайджест 31 декабря и Zero-Lag годовая ротация архива**:
  - 31 декабря в 20:00: праздничный пуш `YearEndDigestWorker.kt` с бокалом 🥂, модальное окно `YearEndDigestDialog` с годовыми итогами (TIR, средний сахар, замеры) и экспорт открытки в PDF (`YearEndReportPdfGenerator.kt`).
  - Архитектура Zero-Lag: автоматическое запечатывание прошлых календарных лет в `Documents/TIRUp/Backups/tirup_readings_YYYY.csv`, исключающее замедление приложения и базы при многолетнем использовании (1–5+ лет).
- **Семейный BLE-мост (Family BLE Bridge, 100% Offline)**:
  - Прямая передача гликемии, тренда, IoB и батареи по BLE раз в 60с на 10–15м без интернета и сопряжения.
  - Роли «Вещатель» и «Наблюдатель», защита 3-значным Family PIN, статус «RX» в ожидании первого пакета, компактные тосты с индикатором сигнала (`🟢 BLE: 🩸7.8 →, 💉1.5, 🔋85%`).
- **UI/UX полировка и надёжность годового архива (v2.1.1)**:
  - Унифицирован дизайн всех кнопок резервного копирования и диалогов к синему стилю `ActionBlue OutlinedButton` («Создать», «Поделиться», «В Zip-файл», «Восстановить», «Итоги года и архив», «Выписка PDF», «Открытка в PDF», «В архив / Обновить», «Закрыть»).
  - Устранено дублирование всплывающих тостов (Snackbar) при открытых модальных окнах.
  - В диалог «Итоги года» добавлен переключатель годов `[ ◀ YYYY ▶ ]` и информационная подсказка об авто-генерации 31 декабря в 20:00.
  - Реализован catch-up в `YearEndDigestWorker`: если телефон был выключен 31 декабря, архив и итоги сформируются автоматически при первом включении устройства в январе.
  - После ручной архивации года выводится тост с кнопкой «Открыть» для быстрого перехода к `tirup_readings_YYYY.csv`.
  - В PDF-отчёте AGP стандартизирован вывод в шапке `Лаб. HbA1c: 6.4% (ДД.ММ.ГГГГ)` без дублирования в блоке параметров.
- **Авто-восстановление плавающего кружка, точный переход к архиву и калибровка годов (v2.1.2)**:
  - Авто-восстановление сервиса `FloatingBubbleService`: при обновлении APK (`ACTION_MY_PACKAGE_REPLACED`), перезагрузке (`BOOT_COMPLETED`), запуске процесса `TirupApplication` и возврате в `MainActivity.onResume` активный плавающий кружок мгновенно поднимается без необходимости вручную передёргивать тумблер в настройках.
  - Исправлена работа кнопки «Открыть» в `openSavedFileFolder`: для архивов и файлов вне папки Downloads (`Documents/TIRUp/Backups/`) системный проводник открывает именно целевую папку или запускает ассоциацию с файлом, вместо безусловного открытия папки «Загрузки».
  - Переключатель архива годов в диалоге «Итоги года» ограничен минимальным порогом 2025 года (`2025..текущий год`).
- **Сквозное код-ревью: грамматика, PluralUtils, воркер и жизненный цикл ресивера (v2.1.3)**:
  - Создан централизованный утилитный класс `PluralUtils` для корректного склонения русских числительных (`formatEvents`, `formatYears`, `formatDays`).
  - Исправлена ошибка «1 событий» в блоке паттернов `TrendsScreen.kt` (теперь строго: `1 событие`, `2 события`, `5 событий`).
  - Исправлены склонения возраста и стажа диабета (`21 год`, `24 года`, `25 лет`) в `AgpPdfGenerator.kt`, `AgpSheetPreviewModal.kt`, `Hba1cReportPdfGenerator.kt` и `SettingsScreen.kt`.
  - Исправлены склонения серий дней в `StreakMotivatorDialog.kt`.
  - В `YearEndDigestWorker.kt` устранено потенциальное зацикливание фонового воркера при пустой базе: `lastYearEndDigestShownYear` теперь сохраняется корректно вне зависимости от наличия замеров.
  - В `BackupAlarmReceiver.kt` добавлен `goAsync()` с гарантированным вызовом `finish()` в `finally`, что предотвращает досрочное уничтожение процесса Android при фоновом старте плавающего виджета.
  - В `ReportsScreen.kt` стандартизирован MIME-тип CSV на `"text/csv"`.
  - В руководстве пользователя `UserManualPdfGenerator.kt` устаревшее слово «запечатывание» заменено на «сохранение в архив», исправлено написание буквы «ё» в слове «расчётным».
  - Добавлены unit-тесты `PluralUtilsTest.kt`.

---

### [Completed] 3. Независимый слайдер громкости тревог, настраиваемый снуз, мультифайловый импорт и редизайн виджета 5х1 (v2.2.0)
- **Независимый регулятор громкости тревог (20% – 100%)**:
  - Отдельный плавный слайдер в настройках звука (`alertVolumePercent`), позволяющий регулировать силу звука тревог независимо от системной громкости звонков.
  - Кнопка «Тест 🔔», проигрывающая реалистичную полифоническую мелодию тревоги на выставленной громкости.
- **Быстрая пауза всех тревог (Snooze All: 10м .. 8ч)**:
  - Интерактивный селектор на экране «Фокус» с пресетами `10м`, `30м`, `1ч` (по умолчанию), `2ч`, `4ч`, `8ч`.
  - Динамическая кнопка «Пауза 1 ч ⏸️», мгновенно ставящая все уровни тревог на паузу.
  - Автоматическое глушение сигнала потери связи сенсора на время активной паузы.
  - Двусторонний контекстный перевод журнала тревог (Alert Log) и таймеров пауз на лету при смене языка (RU/EN).
- **Мультифайловый пакетный импорт исторических данных из xDrip/CSV/ZIP**:
  - Поддержка единовременного выбора неограниченного числа файлов в системном диалоге (пакетная загрузка архивов ZIP, CSV отчетов xDrip+ и Dexcom Clarity).
  - Потоковая распаковка и парсинг без перегрузки памяти, авто-определение форматов дат и разделителей, фоновая дедупликация в Room.
- **Редизайн виджета 5х1 и устранение обрезания текста**:
  - Рокировка метрик: постоянное отображение ключевой клинической триады диапазонов рядом — `TIR` в столбце 2, `TBR` (гипо, цель < 4%) и `TAR` (гипер) в столбце 3.
  - Динамические слоты столбцов 4 и 5: приоритет оперативных данных (IoB 💉, батарея мастера 🔋, стрик 🔥, время ⏱), со 100% надежным заполнением свободного места суточной статистикой (`Ср: 7.4` / `Avg: 7.4`, `CV: 28%`, `TING`, `GMI`).
  - Оптимизация отступов в XML макете виджета (+21 dp ширины), устранившая появление многоточия `...`.
  - Мгновенная реакция виджетов на смену языка и единиц измерения на лету через `updateAllWidgets()`.
  - Обновление растрового файла предпросмотра `widget_strip_preview.png` для совместимости со старыми лаунчерами (Android $\le 11$).
- **Презентационный пост для Telegram**:
  - Подготовлен структурированный готовый пост с обзором функционала и преимуществ TIRUp для сообщества (`.agent/docs/TELEGRAM_POST.md`).

---

### [Completed] 4. Стилизация UI, постоянные сроки расходников, очистка батарейки и аппаратный BLE Keep-Alive Engine (v2.2.1)
- **Оформление кнопок и чипов**:
  - Кнопка «HbA1c» в отчётах: синяя рамка и текст `ActionBlue`, векторная иконка колбы `Icons.Default.Science` цвета `Color(0xFFEF4444)`.
  - Открытие журнала HbA1c: прямой показ `Hba1cHistoryDialog` поверх главного экрана без перехода на экран настроек в фоне.
  - Чип расходников: гармоничный синий контур `ActionBlue.copy(alpha = 0.45f)`.
- **Защита и выбор даты расходников**:
  - Сохранение выставленных сроков службы (дней) для сенсора, канюли и ланцета в `SettingsRepository` без сброса при закрытии диалога.
  - Режим блокировки от случайного изменения (тап по иконке карандаша для редактирования).
  - Полноценный `DatePickerDialog` в `CountdownConfirmDialog` для выбора точной прошедшей даты установки устройства.
- **Очистка виджетов и уведомлений от «🔋 ?»**:
  - Скрытие слота батареи при отсутствии данных или устаревании (> 5 мин), освобождающее место для стрика, IoB/CoB и клинических индексов.
- **Аппаратный Keep-Alive и Anti-Throttling для BLE-моста**:
  - Полное решение проблемы засыпания Слушателя и обрыва связи через 20–30 минут на Android 8–16 (Doze mode и AOSP scan demotion limit).
  - Аппаратный 5-минутный цикл `AlarmManager` (RTC_WAKEUP) с безопасным fallback для Android 12+ (`setAndAllowWhileIdle()`).
  - Удержание системного `WakeLock` внутри `BleScanKeepAliveReceiver` через `goAsync()`, гарантирующее завершение цикла рестарта сканера до ухода процессора в сон.
  - Единая точка контроля здоровья сканера под `Mutex`: реактивная проверка тишины ($\ge 6$ минут) и проактивный рестарт при возрасте скана $\ge 20$ минут для обхода 30-минутного лимита AOSP.
  - Защита от спама и лимита AOSP «5 вызовов за 30 сек» (кулдаун 60 сек, пауза 350 мс для IPC стека, создание свежего `ScanCallback` на каждый старт).
  - Успешный часовой полевой тест на OnePlus 13 под Doze mode (12 тиков из 12 без единого сбоя).



---

# Архив выполненных итераций (1–21): Комплексный пакет багфиксов, алгоритмы DiaKiaBot, BLE Keep-Alive, UX бэкапов и центрированная навигация

# План реализации: Комплексный пакет багфиксов и синхронизации вычислений (8 пунктов)

## 1. Задачи по пунктам багрепорта

### Пункт 1. [Completed] Руководство пользователя (`UserManualPdfGenerator.kt`)
- Добавить в Секцию 6 информацию об отслеживании расходных материалов:
  - Счётчики срока службы сенсора CGM, инфузионного набора/канюли помпы и ланцета прокалывателя.
  - Напоминания об истечении срока, автоопределение смены канюли из заметок xDrip+, цветовая индикация (зелёный >24ч, жёлтый ≤24ч, красный при просрочке).

### Пункт 2. [Completed] Стабилизация ввода ФИО в профиле (`SettingsScreen.kt`)
- Введено локальное состояние `remember(profile) { mutableStateOf(profile) }` в `PatientProfileEditDialog`.
- Убран триггер асинхронной записи в БД и тяжелого автобэкапа на каждый ввод символа.
- Применение изменений профиля происходит локально и синхронно без скачков курсора и удаления слов.

### Пункт 3. [Completed] Исправление тавтологии в рекомендации ланцета (`DeviceStatusModal.kt`)
- В строке 278 заменена фраза:
  `"травмирует пальцы и может стать источником микротравм кожи"` на:
  `"вызывает болезненные ощущения и повреждает кожу подушечек пальцев"`.

### Пункт 4. [Completed] Синхронизация и улучшение отсчёта времени устройств (`DeviceStatus.kt`, `DeviceStatusModal.kt`, `DeviceStatusChips.kt`)
- Устранена проблема «залипания 1д»: добавлена функция `formatDeviceRemainingTime` с дробным отображением суток (например, `1.5д`) или часов в модалке (`1д 12 ч`).
- Синхронизирована логика между чипами и модальным окном.

### Пункт 5. [Completed] Скрытие Семейного PIN под спойлер-замазку в BLE-модале (`FocusScreen.kt`)
- Реализован компонент `SpoilerPinBadge` со скрытием PIN под точками (`•••`) и переключением видимости по тапу с иконкой глаза.

### Пункт 6. [Completed] Очистка карточек параметров от 3-й строки с единицами (`Components.kt`)
- В `BentoMetricCompact` удалена 3-я строка с единицами измерения (`unit`), улучшена читаемость карточек.

### Пункт 7. [Completed] Кликабельный путь сохранения файлов с открытием папки (`ReportsScreen.kt`)
- Заменён системный Toast на `Snackbar` с действием «Открыть» (`openSavedFileFolder`) через `DownloadManager.ACTION_VIEW_DOWNLOADS` и `FileProvider`.

### Пункт 8. [Completed] 100% синхронизация алгоритмов расчета с DiaKiaBot (`StreamingGlucoseImporter.kt`, `GlucoseMetricsCalculator.kt`, `DexdripBroadcastReceiver.kt`, `WeeklyDigestCalculator.kt`)
- Удалена лишняя дедупликация в стриминговом импортере (`seenTimestamps`), восстановив полный набор точек (6561).
- Приведен коэффициент `MGDL_FACTOR` к `18.0182`.
- Синхронизированы полуинтервалы TIR, TING, TBR, TAR, диапазоны GRI и формула xDrip `tir = 100 - tbr - tar`.

## 2. Итерация 2: [Completed] Доработка модальных окон, Snackbar и спойлера PIN (Пункты 5 и 7)

### Пункт 5. [Completed] Спойлер PIN в модальном окне «BLE-мост: Приёмник/Вещатель» (`FocusScreen.kt`, `SettingsScreen.kt`)
- В `FocusScreen.kt` в диалоге `BleStatusDialog` открытый PIN удален из всех текстовых шаблонов описания.
- Добавлена аккуратная плашка `Семейный PIN: [ SpoilerPinBadge ]`, скрывающая PIN под `•••` и открывающая его по нажатию.
- В `SettingsScreen.kt` в плитке настроек отображается реальный установленный PIN (`Код: $familyPin`).

### Пункт 7. [Completed] Snackbar внутри диалогов и «Сохранить руководство» (`ReportsScreen.kt`, `AgpSheetPreviewModal.kt`, `HelpAndDisclaimerDialog.kt`, `SettingsViewModel.kt`, `SettingsScreen.kt`)
- В `ReportsScreen.kt` и `AgpSheetPreviewModal.kt`:
  - Встроен `SnackbarHost` внутрь `ParametersGuidebookModal` и `AgpSheetPreviewModal`, Snackbar с кнопкой «Открыть» отображается сразу при сохранении без необходимости закрывать модальное окно.
- В `HelpAndDisclaimerDialog.kt`:
  - Кнопка переименована в «Сохранить руководство» (EN: «Save User Manual»).
  - Убран лишний эмодзи `📄`, используется единственная векторная иконка `Download`.
  - Встроен `SnackbarHost` для отображения статуса сохранения прямо в диалоге.
- В `SettingsViewModel.kt` и `SettingsScreen.kt`:
  - Добавлен метод `saveUserManualToDownloads()` с сохранением PDF руководства в системную папку «Загрузки».
  - Подключен `Snackbar` с действием «Открыть» через `openSavedFileFolder`.

## 3. Итерация 3: Заголовок дайджеста и постоянный плавающий кружок

### Этап 1. [Completed] Заголовок «Недельный дайджест» в шторке и баннере
- `GlucoseAlertManager.kt`: добавлены `setSubText("Недельный дайджест")`, `setContentTitle("Недельный дайджест")`, `setContentText(digest.headline)`.
- `TrendsScreen.kt`: в `WeeklyDigestBanner` строго сохранены 2 строки:
  - Строка 1: `📊 НЕДЕЛЬНЫЙ ДАЙДЖЕСТ` (11sp, bold, ActionBlue).
  - Строка 2: `digest.headline` (13.5sp, semi-bold).

### Этап 2. [Completed] Постоянно плавающий кружок (50% мини-режим в норме) + документация
- **2.1. Модель данных и репозиторий**:
  - `UserSettings.kt`: добавлен флаг `isFloatingBubbleAlwaysVisible: Boolean = false`.
  - `SettingsRepositoryImpl.kt`: сохранение/загрузка `KEY_IS_FLOATING_BUBBLE_ALWAYS_VISIBLE`.
  - `AutoBackupManager.kt`: поддержка ключа `isFloatingBubbleAlwaysVisible` при резервном копировании.
  - `SettingsViewModel.kt`: метод `toggleFloatingBubbleAlwaysVisible(enabled: Boolean)`.
- **2.2. Настройки (UI)**:
  - `SettingsScreen.kt`: чекбокс/свитч «Отображать постоянно» с подробным пояснением логики мини-режима и действий по тапу/удержанию.
- **2.3. Логика FloatingBubbleService**:
  - Адаптивный размер `WindowManager.LayoutParams`: `40dp` в мини-режиме (без перекрытия полезной площади экрана кликами) и `76dp` в режиме тревоги.
  - В норме (3.9–10.0): мини-режим 50% (диаметр кружка 36dp, скрытие стрелки, шрифт сахара 12sp, зелёный/изумрудный контур), тап открывает `MainActivity`.
  - Вне нормы (< 3.9 или > 10.0): полный размер (60dp, стрелка, сирена/волны), короткий тап глушит/снузит, длинный тап (≥500мс) глушит и открывает приложение.
- **2.4. Документация**:
  - `HelpAndDisclaimerDialog.kt`: описание функции в карточке умных тревог.
  - `UserManualPdfGenerator.kt`: описание в PDF-руководстве пользователя.

## 4. Итерация 4: [Completed] Кластеризация меток инсулина/углеводов (≤5 мин) и эшелонирование («лесенка») на суточном графике
- **4.1. Модель TreatmentCluster и алгоритм кластеризации (`DailyGlucoseChart.kt`)**:
  - Создана модель `TreatmentCluster` и функция `clusterTreatments(treatments, isInsulin, thresholdMs = 5 мин)`.
  - Отметки одного типа с разницей ≤ 5 минут объединяются в один компактный бейдж вида `4+2U` / `20+15g` (или `12U (+)` / `65g (+)` при превышении 8 символов).
- **4.2. Эшелонирование «Лесенка» (Level 0 / Level 1)**:
  - При визуальном перекрытии по горизонтали (при текущем зуме) метки автоматически разносятся по высоте:
    - Инсулин: Level 0 (`chartTop + 6f`), Level 1 (`chartTop + 30f`).
    - Углеводы: Level 0 (`chartBottom - 26f`), Level 1 (`chartBottom - 50f`).
  - Вертикальные пунктирные направляющие аккуратно связывают метки обоих уровней со шкалой графика.
- **4.3. Инспектор по тапу (нижняя плашка)**:
  - Интерактивный тап учитывает вертикальную полуплоскость (Y-preference) для точного выбора между инсулином и углеводами при их совпадении по времени.
  - Для кластеров отображается детальный состав (например, `⏱ 14:00 • 💉 4+2=6 Ед (2 подколки)`), кнопка удаления удаляет все подколки/приёмы пищи в кластере.
- **4.4. Юнит-тестирование (`TreatmentClusterTest.kt`)**:
  - Покрыты тестами граничные условия: одиночные метки, группировка до 5 минут, разделение меток >5 минут, fallback при длинных строках, combo-события.

## 5. Итерация 5: [Completed] Плавная анимация трансформации и калибровка порогов плавающего кружка (`FloatingBubbleService.kt`)
- **5.1. Устранение застревания в увеличенном режиме при 3.9 / 10.0 ммоль/л**:
  - При активном чекбоксе «Отображать постоянно» расчет `isOutOfRange` строго следует целевому диапазону `3.9 .. 10.0` (без гистерезиса 4.1/9.8), гарантируя мгновенный переход в 50% мини-режим при любом сахаре в норме.
- **5.2. Аппаратная анимация масштабирования (Scale Pop-up & Shrink)**:
  - **Выход из нормы (Mini 34dp -> Alarm 60dp)**: пружинящая анимация масштабирования с `OvershootInterpolator(1.25f)` (300 мс), кружок плавно «выпрыгивает» из мини-режима с привязкой точки масштабирования `pivotX` к прилегающему краю экрана.
  - **Возврат в норму (Alarm 60dp -> Mini 34dp)**: плавное сжатие в 2 раза (`DecelerateInterpolator`, 250 мс) с последующей компактной фиксацией окна `WindowManager` и сохранением безопасного расстояния от краев дисплея.

## 6. Итерация 6: [Completed] Кольцевой суточный TIR на плавающем кружке, Local Nightscout в ROADMAP и бамп версии 2.0.9
- **6.1. Кольцевой прогресс суточного TIR (`FloatingBubbleService.kt`)**:
  - Создан кастомный `TirRingDrawable` с фоновой темной заливкой (`#F00F172A`), темным треком (`#334155`) и круговой дугой суточного TIR со скругленными краями (`Paint.Cap.ROUND`).
  - В фоновом корутинном потоке (`Dispatchers.IO`) вычисляется суточный TIR (`calculateTodayTir`) на основе всех показаний с 00:00 текущих суток через `GlucoseMetricsCalculator.calculateStatistics`.
  - При сахаре в норме (3.9–10.0 ммоль/л) зелёный/изумрудный ободок наглядно отображает дугу суточного процента TIR (0..100%). При тревоге (гипо/гипер) контур замыкается на 100% тревожным цветом.
- **6.2. Local Nightscout & P2P-перенос данных (`ROADMAP.md`)**:
  - В дорожную карту развития TIRUp добавлен Секция 6: «Автономный локальный Nightscout (Local Nightscout & P2P-перенос данных без интернета)».
  - Описаны: архитектура Zero-Data-Loss, P2P Wi-Fi Direct/LAN мастер миграции со старого смартфона на новый, локальный HTTP REST API сервер (`:17580`) для локального экосистемного сопряжения без внешнего облака.
- **6.3. Версионирование (`build.gradle.kts`)**:
  - Обновлены параметры сборки: `versionCode = 11`, `versionName = "2.0.9"`.

## 7. Итерация 7: [Completed] Полноценный предпросмотр всех 8 виджетов рабочего стола (`android:previewLayout` + `android:previewImage`)
- **7.1. Поддержка предпросмотра для всех лаунчеров (`android:previewLayout` + `android:previewImage`)**:
  - Для всех 8 типов виджетов (`widget_2x1`, `widget_3x1`, `widget_compact`, `widget_dashboard`, `widget_dashboard_medium`, `widget_minimal`, `widget_strip`, `widget_vertical`) в `xml/widget_*_info.xml`:
    - Прописан `android:previewLayout="@layout/widget_..."` для современных AOSP / Google Pixel лаунчеров на Android 12+ (API 31+).
    - Заменён дефолтный `android:previewImage="@mipmap/ic_launcher"` на индивидуальные графические превью `@drawable/widget_*_preview` в папке `res/drawable-nodpi/`.
    - Это обеспечивает 100% отображение живого вида виджетов в фирменных оболочках Xiaomi HyperOS / MIUI, Samsung One UI, Oppo ColorOS и сторонних лаунчерах, которые считывают именно `previewImage`.
- **7.2. Векторный график гликемии для предпросмотра (`widget_preview_chart.xml`)**:
  - Создан векторный drawable `widget_preview_chart.xml` со сглаженной гликемической волной целевого диапазона, полосой нормы (3.9–10.0 ммоль/л), точками измерений и светящейся точкой текущего сахара.
  - Подключен в качестве дефолтного источника `android:src="@drawable/widget_preview_chart"` для графиков в `widget_dashboard.xml` и `widget_dashboard_medium.xml`.
- **7.3. Калибровка макетов под предпросмотр (`widget_*.xml`)**:
  - В `widget_2x1.xml`, `widget_3x1.xml`, `widget_compact.xml`, `widget_strip.xml`, `widget_vertical.xml` бейджи IoB, Streak и Compensator выставлены в видимое состояние по умолчанию для демонстрации полного набора возможностей виджета в каталоге лаунчера (при добавлении на экран `TirupWidgetUpdater` динамически переключает видимость согласно реальным настройкам пользователя).

## 8. Итерация 8: [Completed] 3-секундное удержание для отключения постоянного кружка (`FloatingBubbleService.kt`)
- **8.1. Обратный отсчёт и распознавание жеста (`FloatingBubbleService.kt`)**:
  - При удержании пальца на мини-кружке в норме (3.9–10.0 ммоль/л) свыше 450 мс запускается таймер обратного отсчёта на 3 секунды.
  - Каждую секунду выводится всплывающее сообщение с тактильным откликом (вибро-тик): `⏳ Удерживайте ещё 3 сек. для отключения` ➔ `2 сек` ➔ `1 сек`.
  - При отпускании пальца раньше истечения 3 секунд или сдвиге кружка (`dx > 20px || dy > 20px`) таймер немедленно отменяется, а открытие `MainActivity` блокируется, исключая ложный переход в приложение при отмене.
- **8.2. Автоматическое отключение и сохранение настройки**:
  - По истечении 3 полных секунд срабатывает подтверждающий виброотклик (120 мс), выводится Toast `«Постоянный кружок отключён»`.
  - В репозитории настроек сохраняется `isFloatingBubbleAlwaysVisible = false`.
  - Кружок немедленно исчезает с экрана (так как сахар в норме), но настройка тревог остаётся активной — при выходе сахара из нормы (< 3.9 или > 10.0) кружок появится снова.
- **8.3. Актуализация описания в UI настроек (`SettingsScreen.kt`)**:
  - Подзаголовок опции «Отображать постоянно» дополнен информацией о быстром отключении кружка удержанием 3 секунды.

## 9. Итерация 9: [Completed] Гарантированная сохранность локальной БД и перенос данных на новый смартфон (Zero-Data-Loss & Migration)
- **9.1. Расширение структуры бэкапа и поддержка меток (`AutoBackupManager.kt`, `TreatmentDao.kt`)**:
  - Включение `TreatmentEntity` (инсулин, углеводы, заметки) в JSON-бэкап (`"treatments": [...]`) наряду с `GlucoseReadingEntity` и `UserSettings`.
  - Потоковое чтение и запись порциями для исключения OOM при больших базах данных.
  - Безопасное сохранение в публичную системную папку `Documents/TIRUp/Backups/` (не удаляется при удалении/переустановке TIRUp) с автоматическим fallback на `Android/data/.../files/Backups/`.
  - Методы экспорта в `OutputStream` и чтения из `InputStream` для бесшовной работы с SAF (`content://` URI).
- **9.2. Бизнес-логика экспорта, импорта и шеринга (`SettingsViewModel.kt`)**:
  - `createBackupNow()`: ручное создание актуального бэкапа в 1 клик без ожидания 00:00.
  - `exportBackupToUri(uri: Uri)`: запись JSON в произвольный файл через системный `CreateDocument`.
  - `shareBackup()`: передача файла резервной копии через системное меню `Поделиться` (Telegram, почта, Bluetooth, Quick Share) через `FileProvider`.
  - `prepareRestoreFromUri(uri: Uri)`: быстрое чтение заголовка для превью перед восстановлением.
  - `confirmRestore(uri: Uri)`: восстановление замеров, меток и настроек с пакетной вставкой в Room.
- **9.3. UI карточки автобэкапа внизу настроек (`SettingsScreen.kt`)**:
  - Информационная плашка: статус последнего бэкапа, количество замеров сахара и меток терапии, путь хранения (`Документы/TIRUp/Backups/`).
  - Ряд действий:
    - Кнопка «Создать бэкап сейчас» (ручной снапшот).
    - Кнопка «Поделиться» (отправка на другой телефон по Bluetooth/мессенджером).
    - Кнопка «Сохранить в файл» (SAF CreateDocument).
    - Кнопка «Восстановить из файла» (SAF OpenDocument).
  - Модальное окно подтверждения восстановления: отображение сводки бэкапа (ФИО, период данных, число точек), предупреждение о слиянии, кнопки «Отмена» и «Восстановить».

## 10. Итерация 10: Квартальный рубеж HbA1c, журнал анализов и годовая ротация бэкапов

### 10.1. Этап 1: [Completed] Модели данных, журнал HbA1c, плашка в профиле и экспорт выписки в PDF
- **Модель данных (`UserSettings.kt`, `AutoBackupManager.kt`)**:
  - `data class LabHba1cRecord(val id: Long, val timestamp: Long, val valuePercent: Double, val labName: String = "", val notes: String = "")`.
  - Добавление `val hba1cRecords: List<LabHba1cRecord> = emptyList()` и `val isHba1cReminderEnabled: Boolean = true` в `UserSettings`.
  - Поддержка чтения и записи списка `hba1cRecords` в `tirup_settings.json` и обратная совместимость со старыми бэкапами.
- **Бизнес-логика в ViewModel (`SettingsViewModel.kt`)**:
  - Метод добавления нового анализа `addHba1cRecord(...)`.
  - Метод удаления анализа `deleteHba1cRecord(id)`.
  - Расчёт текущего 90-дневного GMI по сенсору для сопоставления с последним лабораторным значением.
- **UI плашки в карточке профиля (`SettingsScreen.kt`)**:
  - Справа от «Мой профиль» в `PatientProfileSummaryCard`: компактный бейдж с красной рамкой (`BorderStroke(1.2.dp, Color(0xFFEF4444))`).
  - Текст: `🩸 HbA1c: 6.4%` (или `HbA1c: +`, если анализов ещё нет).
  - Клик по плашке открывает модальное окно журнала анализов (не открывая редактирование профиля).
- **Модальное окно «Лабораторный HbA1c и GMI» (`Hba1cHistoryModal.kt` / `SettingsScreen.kt`)**:
  - Карточка сопоставления: Последний лабораторный HbA1c vs Расчётный 90-дневный GMI (разница $\Delta$, оценка сходимости).
  - Форма быстрого ввода: дата, значение в %, лаборатория, заметка.
  - Список ранее введённых анализов с датами и значениями.
  - Кнопка «Экспорт в PDF» (создание аккуратной выписки для врача).
- **Генератор PDF-справки (`Hba1cReportPdfGenerator.kt`)**:
  - Формирование аккуратной 1-страничной выписки: ФИО, таблица динамики лабораторного HbA1c, сравнение с 90-дневным GMI и TIR, график/тренд сходимости.

### 10.2. Этап 2: [Completed] Квартальное напоминание (раз в 90 дней)
- **Точка отсчёта**: `maxOf(earliestReadingTimestamp, latestRecordTimestamp, skippedQuarterTimestamp)`.
- **Защита от спама и лимит напоминаний**:
  - Строго не более 2-х уведомлений за цикл (первое при $\ge 90$ дней, повторное через 14 дней; далее прекращение пушей до нового анализа или сброса).
  - Кнопка «Пропустить» (+90 дней) прямо в шторке уведомлений и в модальном окне журнала HbA1c (для пользователей, решивших отложить сдачу).
- **Поддержка ретроспективных анализов**:
  - Редактируемое поле даты в модальном окне журнала с валидацией различных форматов (ДД.ММ.ГГГГ, ГГГГ-ММ-ДД и т.д.) для ввода анализов, сданных задолго до установки TIRUp.
- **Фоновый воркер (`Hba1cReminderWorker.kt`)**:
  - Суточная периодическая проверка срока и расчёт 90-дневного GMI.
  - Канал уведомлений `CHANNEL_HBA1C_REMINDER` с действиями «Журнал HbA1c» и «Пропустить».
- **Интеграция с UI и бэкапами**:
  - Тумблер включения/выключения напоминаний в настройках.
  - Сохранение параметров цикла (`hba1cSkippedQuarterTimestamp`, `hba1cRemindersCountInCycle`, `lastHba1cReminderTimestamp`) в `UserSettings` и JSON-бэкапах.

### 10.3. Этап 3: [Completed] Новогодний дайджест 31 декабря и годовая ротация архива
- **3.1. Годовая ротация архивов и Zero-Lag на 1–5 лет (`AutoBackupManager.kt`)**:
  - Экспорт завершившихся лет в неизменяемые файлы `Documents/TIRUp/Backups/tirup_readings_YYYY.csv`.
  - Ограничение активного бэкапа `tirup_backup_active.json` текущим скользящим годом (365 дней) + полные метки терапии и настройки.
  - Механизм автоматической ротации при наступлении нового календарного года.
- **3.2. Новогодний воркер и уведомление 31 декабря (`YearEndDigestWorker.kt`, `GlucoseAlertManager.kt`)**:
  - Канал уведомлений `tirup_year_end_v1`, запуск 31 декабря в 20:00.
  - Пуш: расчет годового TIR и GMI, кнопка «Открыть итоги года».
  - Переход по клику на глубокую ссылку/экран итогов года.
- **3.3. Модальное окно и генератор итогов года (`YearEndDigestDialog.kt`, `SettingsScreen.kt`)**:
  - Праздничный интерфейс: годовой TIR, GMI, дни активного мониторинга, лучший месяц по компенсации, Best Streak.
  - Кнопка «Итоги года» в карточке резервного копирования в настройках для повторного просмотра в любое время.
  - Кнопка «Экспорт годовой открытки / отчёта в PDF» (`YearEndReportPdfGenerator.kt`).
- **3.4. Бамп версии приложения (`build.gradle.kts`)**:
  - Обновление: `versionCode = 12`, `versionName = "2.1.0"`.

## 11. Итерация 11: [Completed] Мультифайловый импорт, быстрая 2-часовая пауза тревог и независимая громкость

### Этап 1: [Completed] Мультифайловый импорт исторических данных из xDrip/файлов
- `ReportsScreen.kt`: замена контракта пикера документов на `ActivityResultContracts.OpenMultipleDocuments()`.
- `ReportsViewModel.kt`: метод `importHistoricalFiles(uris: List<Uri>)` для последовательной обработки нескольких выбранных файлов с отображением прогресса («Обработка файла X из Y...») и итогового суммарного числа добавленных точек.
- Сохранение атомарности и предотвращение утечек памяти при работе со стриминговым импортером.

### Этап 2: [Completed] Быстрая пауза тревог на 2 часа в Журнале тревог (Snooze All)
- `AlertSettings.kt`: добавление поля `alertsMuteUntilTimestamp: Long = 0L`.
- `GlucoseAlertManager.kt`: проверка активности паузы (`now < alertsMuteUntilTimestamp`) для приглушения основных, упреждающих тревог и потери связи; синхронизация с `criticalHypoPauseUntilTimestamp`.
- `SettingsRepositoryImpl.kt` & `SettingsViewModel.kt`: методы `pauseAlertsFor(durationMs)` и `resumeAlerts()`.
- `FocusScreen.kt`:
  - В `DailyAlertLogsDialog` добавление верхней карточки быстрого управления:
    - Кнопка «⏸️ Пауза на 2 часа» при активных тревогах.
    - Баннер обратного отсчета («🔕 Пауза: осталось X мин») и кнопка «▶️ Возобновить» при активной паузе.
  - Изменение иконки колокольчика на верхней панели FocusScreen на перечёркнутый `🔕` во время паузы.

### Этап 3: [Completed] Независимый ползунок громкости для некоритических тревог
- `AlertSettings.kt`: добавление поля `alertVolumePercent: Int = 80` (20% .. 100%).
- `SettingsRepositoryImpl.kt` & `AutoBackupManager.kt`: сохранение/загрузка и сериализация ключа `alertVolumePercent`.
- `MedicalSoundPlayer.kt`: масштабирование амплитуды PCM-волны синусоиды для упреждающих (`PREDICTIVE`) и основных (`MAIN`) сигналов согласно `alertVolumePercent`. Критические тревоги (`CRITICAL`, `SIGNAL_LOSS`, `LastChance`) остаются строго на 100% громкости потока `USAGE_ALARM`.
- `SettingsScreen.kt`: слайдер «Громкость упреждающих тревог» (20%–100%) с шагом 5%, кнопкой проверки звука «Тест 🔔» и пояснением о 100% громкости для критических тревог.

## 12. Итерация 12: [Completed] Настраиваемый интервал паузы тревог (10м–8ч) и стабилизация тестового звука

### Этап 1: [Completed] Исправление движка тестового звука (`MedicalSoundPlayer.kt`)
- Устранение бага мгновенного обрыва: сброс флага `isPlayingActive = true` перед запуском трека.
- Добавление прелюдии 50 мс тишины для аппаратного пробуждения ЦАП смартфона.
- Замена короткого одиночного пика на гармоничную 4-нотную восходящую мелодию (~1.0 сек, D5 ➔ G5 ➔ A5 ➔ D6) для точной оценки громкости.
- Гарантированный подъем аппаратной громкости `STREAM_ALARM` минимум до 85% (`ensureAlarmStreamAudible()`).

### Этап 2: [Completed] Настраиваемый выбор интервала паузы тревог в UI (`FocusScreen.kt`, `SettingsScreen.kt`)
- `FocusScreen.kt`:
  - В `DailyAlertLogsDialog` добавление ряда быстрых пресетов: `10м`, `30м`, `1ч` (по умолчанию), `2ч`, `4ч`, `8ч`.
  - Кнопка действия с динамической подписью выбранного времени: `Пауза 1 ч ⏸️` (или `Пауза 30 мин ⏸️`).
  - Передача выбранной длительности в `onPauseAlerts(durationMs)`.
- `SettingsScreen.kt`:
  - Обновление текстового баннера: удаление фиксированного суффикса «(2 ч)», динамический вывод оставшегося времени и времени автоматического возобновления тревог (`HH:mm`).

### Этап 3: [Completed] Санитарная валидация и строгая проверка паузы (`SettingsRepositoryImpl.kt`, `GlucoseAlertManager.kt`, `FocusViewModel.kt`)
- `SettingsRepositoryImpl.kt`: ограничение длительности паузы через `.coerceIn(10 * 60 * 1000L, 8 * 3600 * 1000L)`.
- `GlucoseAlertManager.kt`: учет `isMasterActive` в `checkSignalLoss`, чтобы потеря связи не воспроизводила звук во время активной паузы всех тревог.
- `FocusViewModel.kt`: обновление дефолтной паузы до 1 часа (`3600 * 1000L`).

## 13. Итерация 13: [Completed] Оформление кнопок, защита и выбор даты расходников, очистка виджетов от батарейки с ?

### Этап 1: [Completed] Кнопка HbA1c и Кнопка устройств (Пункты 2 и 3)
- `ReportsScreen.kt`: кнопка HbA1c — синяя рамка и текст `ActionBlue`, мягкий фон `ActionBlue.copy(alpha = 0.08f)`, эмодзи заменён на пробирку `🧪`.
- `DeviceStatusChips.kt`: чип расходников — синяя рамка `ActionBlue.copy(alpha = 0.4f)`, фон `ActionBlue.copy(alpha = 0.08f)`. Стрик сохраняет статусную окраску (`PrimaryEmerald`).

### Этап 2: [Completed] Экран «Устройства и расходники»: запоминание дней, защита и выбор даты (Пункт 5)
- `FocusViewModel.kt`: методы обновления сроков службы расходников `updateSensorDuration(days)`, `updatePumpDuration(days)`, `updateLancetDuration(days)` с мгновенным сохранением в `SettingsRepository`.
- `DeviceStatusModal.kt`:
  - Сохранение выбранных дней при изменении без сброса при закрытии диалога.
  - Защита от случайного редактирования: режим «тап для редактирования» (🔒 заблокировано по умолчанию, разблокировка по нажатию кнопки «Изменить»).
  - Добавление полноценного `DatePickerDialog` в `CountdownConfirmDialog`, позволяющего выбрать любую прошедшую дату установки нового устройства (а не только «Вчера»).
- `FocusScreen.kt`: передача коллбэков обновления длительностей в `DeviceStatusModal`.

### Этап 3: [Completed] Очистка от батарейки со знаком вопроса «🔋 ?» (Пункт 6)
- `TirupWidgetUpdater.kt`: скрывать слот батареи на виджетах, если батарея неизвестна (`battery < 0`) или устарела, чтобы не вытеснять другие важные параметры (стрик, расходники, IOB/COB).
- `GlucoseAlertManager.kt`: в lockscreen-уведомлении скрывать батарейку при отсутствии данных или устаревании.
- `FocusScreen.kt`: в `HeroGlucoseCard` скрывать бейдж батареи, если данных нет или они неактуальны.

## 14. Итерация 14: [Completed] Стабилизация Слушателя BLE-моста (Anti-Throttling & Keep-Alive Engine)

### Этап 1: [Completed] Аппаратный AlarmManager Keep-Alive (5-минутный пульс сквозь Doze)
- `BleScanKeepAliveReceiver.kt`:
  - Аппаратный 5-минутный цикл будильника с самоперепланированием цепочки.
  - Совместимость с Android 12+ (API 31+): проверка `canScheduleExactAlarms()` с безопасным fallback на `setAndAllowWhileIdle()` (не требует опасного разрешения `SCHEDULE_EXACT_ALARM` и гарантированно будит CPU из глубокого Doze).
  - Вызов `BleObserverManager.onKeepAliveTick()`.
- `BleObserverService.kt`: регистрация/отмена аппаратного таймера при жизненном цикле сервиса.
- `AndroidManifest.xml`: регистрация ресивера `BleScanKeepAliveReceiver`.

### Этап 2: [Completed] Единая точка проверки здоровья и защита от гонок в BleObserverManager
- `BleObserverManager.kt`:
  - Метод `onKeepAliveTick()`: объединил в одном аппаратном тике реактивную проверку тишины (>= 6 мин без пакетов) и проактивный сброс 30-минутного лимита AOSP (скан старше 20 мин). Устранены ненадежные корутинные `delay()`, замерзающие при выключенном экране.
  - Единая функция `restartScanInternal(reason: String)` под `mutex.withLock`.
  - Защита от спама (кулдаун 60 сек, предохраняющий от лимита AOSP «5 стартов за 30 сек»).
  - Пауза 350 мс перед стартом для IPC-стека Bluetooth и создание совершенно нового экземпляра `ScanCallback` на каждый рестарт.
  - Детальное логирование кодов ошибок `onScanFailed`.

## 15. Итерация 15: [Completed] Коррекция учёта тишины эфира, приём Heartbeat-пакетов и аппаратная оптимизация BLE

### Этап 1: [Completed] Сохранение `lastPacketReceivedSystemMs` при рестартах сканера
- `BleObserverManager.kt`: в `startScanningInternalLocked` устранена перезапись `lastPacketReceivedSystemMs = now` при каждом рестарте сканера.
- Значение инициализируется текущим временем только при самом первом холодном старте (`lastPacketReceivedSystemMs == 0L`), а при последующих рестартах (по кулдауну, проактивному сбросу 20-минутного лимита AOSP или ошибкам) сохраняет отметку времени реального последнего принятого радиопакета.
- Результат: счётчик `packetSilence` в тиках будильника и вотчдог тишины (`silence watchdog`) измеряют истинное время отсутствия эфира, не обнуляясь ложно каждые 20 минут.

### Этап 2: [Completed] Обработка и логирование Heartbeat/повторных пакетов (`handleScanResult`)
- `BleObserverManager.kt`:
  - `lastPacketReceivedSystemMs = System.currentTimeMillis()` теперь обновляется **до** проверки на дубликат.
  - Если `packet.timestamp <= lastHandledTimestamp`, пакет классифицируется как Heartbeat / дубликат в рамках всплеска (burst) вещателя.
  - Добавлено явное информационное логирование с частотой не чаще раза в 3 секунды:
    `Heartbeat received: ts=${packet.timestamp}, bg=${packet.valueMmol}, rssi=$rssi (duplicate, connection alive)`.
  - Испускается событие в `_packetReceivedEvent`, позволяющее UI (зелёная пульсация радиоволны на `FocusScreen`) подтверждать живую связь.
  - Обновляется `lastRssi` в настройках/диагностике, исключая при этом повторную запись дубликатов в Room DB и спам алертами.

### Этап 3: [Completed] Увеличение IPC-паузы и авто-восстановление при ошибке регистрации (код 2)
- `BleObserverManager.kt`:
  - В `restartScanInternal` пауза между `stopScan()` и созданием нового `ScanCallback` увеличена с 350 мс до 800 мс для надёжной очистки регистрации клиента в IPC-стеке Bluetooth на прошивках ColorOS/OxygenOS/HyperOS.
  - В `onScanFailed` добавлена автоматическая обработка ошибки `SCAN_FAILED_APPLICATION_REGISTRATION_FAILED (2)` с повторной попыткой перезапуска через 2.5 секунды.

### Этап 4: [Completed] Аппаратная подстраховка `MATCH_NUM_MAX_ADVERTISEMENT`
- `BleObserverManager.kt`: в `ScanSettings.Builder()` значение `setNumOfMatches()` изменено с `MATCH_NUM_ONE_ADVERTISEMENT` на `MATCH_NUM_MAX_ADVERTISEMENT` в связке с `CALLBACK_TYPE_ALL_MATCHES` для предотвращения подавления последующих пакетов вещателя в радиочипсетах Qualcomm/MediaTek.

## 16. Итерация 16: [Completed] Аппаратный WakeLock, режим Boost при рестартах (60с), лимит 27 мин и разделение времени радиоконтакта и замера глюкозы

### Этап 1: [Completed] Разделение меток времени радиоконтакта и реального замера глюкозы
- **Модель `BleBridgeSettings` и репозиторий (`SettingsRepositoryImpl.kt`)**:
  - Добавлено поле `val lastRadioContactMs: Long = 0L` и ключ `KEY_BLE_BRIDGE_LAST_RADIO_CONTACT_MS`.
  - В `handleScanResult`:
    - При Heartbeat (повторный пакет) обновляется `lastRadioContactMs = now` (подтверждение живой радиосвязи), но `lastPacketTimestamp` остаётся временем реального измерения сенсора.
    - При новом замере обновляются оба поля: `lastPacketTimestamp = packet.timestamp` и `lastRadioContactMs = now`.
- **Пользовательский интерфейс (`FocusScreen.kt`, `SettingsScreen.kt`)**:
  - Анимированный бейдж `BleBridgeBadge` на главном экране ориентируется на `lastRadioContactMs`: пульсирует и отображает `<1м`, если радиосвязь активна прямо сейчас.
  - В модальном окне `BleStatusDialog` показатели строго разделены на две информационные строки:
    - `• Последний замер: $readingAgeStr` (фактическое время замера сенсора: 4 мин назад, 10 мин назад и т.д., исключая дезинформацию пользователя о свежести сахара).
    - `• Радиосигнал: $contactAgeStr` (свежесть радиоэфира / последнего heartbeat: 5 сек назад, 20 сек назад).
  - В настройках `SettingsScreen.kt` в карточке приёмника отображается `• Радиосигнал: $ageStr` по метке `lastRadioContactMs`.

### Этап 2: [Completed] Аппаратный WakeLock при рестартах сканера сквозь глубокий Doze
- `BleScanKeepAliveReceiver.kt`:
  - Внутри `goAsync()` добавлен захват `PowerManager.PARTIAL_WAKE_LOCK` с таймаутом 15 секунд (`wakeLock.acquire(15_000L)`).
  - Процессор смартфона гарантированно удерживается в бодрствующем состоянии на весь период междуфазной паузы IPC (800 мс) и старта сканирования.
- `BleObserverManager.kt`:
  - Внутри `restartScanInternal` добавлен вспомогательный `PARTIAL_WAKE_LOCK` (15 с), предотвращающий засыпание CPU при перезапусках по вотчдогу тишины или ошибкам Bluetooth-стека.

### Этап 3: [Completed] Активация 60-секундного Boost (`SCAN_MODE_LOW_LATENCY`) при программных перезапусках
- `BleObserverManager.kt`:
  - При вызовах `restartScanInternal` по причинам `alarm_silence_timeout`, `alarm_proactive_reset` и `on_scan_failed_recovery` сканер запускается с флагом `boost = true`.
  - Радиочипсет переключается в 100% duty cycle (`SCAN_MODE_LOW_LATENCY`) на первые 60 секунд после рестарта с экрана в режиме сна, гарантированно захватывая следующий секундный пакет вещателя в Doze.
  - По истечении 60 секунд сканер плавно и незаметно для радиоканала переходит в энергоэффективный режим `SCAN_MODE_BALANCED`.

### Этап 4: [Completed] Смещение проактивного сброса на 27 минут
- `BleObserverManager.kt`:
  - Интервал `PROACTIVE_RESET_INTERVAL_MS` увеличен с 20 до 27 минут (`27 * 60 * 1000L`).
  - Обеспечивает непрерывную сессию сканирования на 35% дольше, сохраняя при этом надёжный 3-минутный буфер безопасности до 30-минутного аппаратного лимита AOSP.

## 17. Итерация 17: [Completed] Устранение двойного рестарта в Doze и стабилизация сканирования в BALANCED

### Этап 1: [Completed] Исключение boost из программных фоновых сбросов сканера
- `BleObserverManager.kt`:
  - В `onKeepAliveTickSuspend()` для `alarm_proactive_reset` (27 мин) и `alarm_silence_timeout` (6 мин) флаг `boost` переведён в `false`.
  - При ошибках регистрации `on_scan_failed_recovery` сброс также выполняется в `boost = false`.
  - Сканер атомарно перезапускается прямо в `SCAN_MODE_BALANCED` внутри 15-секундного аппаратного `WakeLock`. Устранено фатальное удвоение рестартов за 60 секунд.

### Этап 2: [Completed] Аппаратный WakeLock при возврате из ручного boost в BALANCED
- `BleObserverManager.kt`:
  - В `boostJob` блок `Reverting scan mode from boost (LOW_LATENCY) to BALANCED` обёрнут в `PowerManager.PARTIAL_WAKE_LOCK` (15 сек).
  - Исключено засыпание процессора во время фазы IPC `delay(800L)` при ручном запуске теста из UI.

### Этап 3: [Completed] Превентивная очистка зависшего скана при старте сервиса
- `BleObserverManager.kt`:
  - В `startScanningFromService()` добавлен превентивный вызов `stopScanningInternal()` перед стартом сканирования, исключающий зомби-регистрации в стеке Android после обновления APK.

## 18. Итерация 18: [Completed] Оптимизация диалога HbA1c, выписки PDF, синхронизация AGP и таймера сенсора

### Этап 1: Оптимизация высоты диалога HbA1c и гарантированная обратная связь выписки PDF
- `SettingsScreen.kt` (`Hba1cHistoryDialog`):
  - Ограничение высоты контейнера истории анализов: при 3+ записях список оборачивается в компактный контейнер с собственной прокруткой (`heightIn(max = 180.dp)`), исключая вытягивание всего окна ввода за пределы дисплея.
  - Встраивание информирующего баннера с кнопкой «Открыть» (`openSavedFileFolder`) непосредственно внутрь `Hba1cHistoryDialog` при завершении выгрузки PDF.
  - Устранение конфликта `SnackbarHostState` между Scaffold экрана и окном `AlertDialog`.
- `SettingsViewModel.kt`:
  - В `exportHba1cReportToPdf` добавление callback / обновления пути сохранённого файла и гарантированного системного сообщения.
- `MainActivity.kt`:
  - Подключение обработки событий `settingsViewModel.events` при открытии `Hba1cHistoryDialog` из главного экрана.

### Этап 2: Перенос «Лаб. HbA1c» в блоке PDF AGP
- `AgpPdfGenerator.kt`:
  - Удаление `Лаб. HbA1c` из текстовой строки описания пациента (`patientLine`), исключая обрезание строки по ширине листа A4 (555 pt).
  - В блоке «Основные показатели (ATTD / ADA)» на строке «Расчётный eA1c» организация двух сбалансированных колонок:
    - Левая колонка: `Расчётный eA1c (GMI): X% (Y mmol/mol)`
    - Правая колонка: `Лаб. HbA1c: Z% (ДД.ММ.ГГГГ)` (или `—`, если записей нет).

### Этап 3: Синхронизация бланка предпросмотра AGP с PDF
- `AgpSheetPreviewModal.kt`:
  - Приведение формулировок и структуры к 1:1 соответствию с PDF-генератором:
    - Заголовок блока: `ОСНОВНЫЕ ПОКАЗАТЕЛИ (ATTD / ADA)`.
    - Объединение среднего сахара и мин/макс в одну строку: `Средний сахар (Mean) • Мин/Макс: ...`.
    - Добавление двух колонок на строке eA1c: `Расчётный eA1c (GMI)` и `Лаб. HbA1c`.
    - Унификация формулировок вариабельности, GRI и ночного профиля.

### Этап 4: [Completed] Синхронизация отсчёта времени в уведомлении о замене сенсора
- `GlucoseAlertManager.kt`:
  - Замена грубого целочисленного деления суток `(millisRemaining / 86_400_000L).toInt()` на вызов единой функции `com.tirup.app.domain.model.formatDeviceRemainingTime`.
  - Уведомление выводит точное согласованное время (например, «Осталось: 1д 4 ч» вместо «1 дн.»), совпадающее с таймером в приложении.

## 19. Итерация 19: [Completed] Очистка уведомлений выписки, стандартизация ширины диалогов, пересчёт сроков расходников, ФИО в отчётах и документация

### Этап 1: [Completed] Очистка уведомлений выписки PDF и стандартизация модальных окон
- `SettingsScreen.kt` (`Hba1cHistoryDialog`):
  - Удалён дублирующий Toast и статический Surface-баннер; сохранён единственный аккуратный Snackbar с кнопкой «Открыть» (`SnackbarDuration.Short`).
  - Удалён верхний крестик закрытия `✕` для поддержания единого визуального стиля окон приложения.
  - Расширен диалог HbA1c до стандартной ширины модалок ввода данных (`properties = DialogProperties(usePlatformDefaultWidth = false)` и отступы по 16dp с краев дисплея, как в `DeviceStatusModal`).
- `SettingsScreen.kt` (`PatientProfileEditDialog`):
  - Расширено модальное окно профиля до полноценной рабочей ширины через `DialogProperties(usePlatformDefaultWidth = false)`.

### Этап 2: [Completed] Моментальный пересчёт текущего состояния сенсора, канюли и ланцета при смене срока
- `FocusViewModel.kt`:
  - В методах `updateSensorDuration`, `updatePumpSetDuration`, `updateLancetDuration` обновляется не только `lastUsedDurationDays`, но и активный `durationDays` установленного устройства.
  - Мгновенный автоматический пересчёт `expiresAt`, оставшегося времени, статуса просрочки и цвета плашки в `DeviceStatusModal` прямо при нажатии `+` / `-`.

### Этап 3: [Completed] Вывод полного ФИО в отчётах и выписках с адаптивным сокращением терапии
- `AgpPdfGenerator.kt` & `AgpSheetPreviewModal.kt`:
  - Использование полного ФИО (`profile.fullName`).
  - Адаптивное компактное сокращение типа терапии («Помпа», «Ручки», «Таблетки», «Диета») для гарантированного размещения длинных 3-составных ФИО на одной строке без переносов и перекрытий.
- `Hba1cReportPdfGenerator.kt`:
  - Корректный вывод полного ФИО с динамическим позиционированием возраста и диагноза.
- `SettingsScreen.kt` (`PatientProfileSummaryCard`):
  - В карточке профиля в Настройках сохранено компактное имя (ФИ).

### Этап 4: [Completed] Расширенное описание работы BLE-моста в руководстве пользователя
- `UserManualPdfGenerator.kt`:
  - В раздел BLE-моста добавлено техническое разъяснение работы в режиме сна: ограничение сканирования чипсета в Android Doze, назначение BLE-моста для улицы и дороги, рекомендация использования Wi-Fi фолловера ночью.
  - Сохранение строго 2-страничного формата PDF.

### Этап 5: [Completed] Актуализация документации и README.md
- `README.md`:
  - Добавлен раздел об учёте расходных материалов и устройств (Supplies & Device Tracker) в русской и английской версиях.
  ## 20. Итерация 20: [Completed] Улучшение UX восстановления данных и актуализация превью виджетов

### Этап 1: [Completed] Окно выбора варианта восстановления и гид по файлам (`SettingsScreen.kt`, `SettingsViewModel.kt`, `AutoBackupManager.kt`)
- `AutoBackupManager.kt`:
  - Реализован метод `restoreLatestAutoBackup(context, database, settingsRepository)` с возвратом подробного `BackupRestoreResult` (число замеров сахара, меток терапии и статус восстановления настроек). Метод автоматически находит и восстанавливает самую свежую резервную копию из `Documents/TIRUp/Backups/` или внутреннего хранилища (как многофайловые `csv/json`, так и `tirup_backup.json` / `zip`).
- `SettingsViewModel.kt`:
  - Добавлен метод `restoreLatestAutoBackup()` с индикатором прогресса и информационным событием `SettingsEvent.Info` с подробным отчетом пользователю.
- `SettingsScreen.kt`:
  - Кнопка «Восстановить» теперь открывает удобное модальное окно `RestoreOptionsModal`:
    - Карточка обнаруженной резервной копии: дата сохранения, количество замеров и меток с кнопкой «⚡ Восстановить в 1 клик».
    - Шпаргалка-гид по файлам: подробно разъяснено назначение каждого типа файла (`tirup_backup_*.zip` или `.json` — полное восстановление; `tirup_readings.csv` — только замеры без сброса настроек; `tirup_treatments.csv` — метки терапии; `tirup_settings.json` — настройки и профиль).
    - Кнопка «Выбрать файл в папке...» запускает системный `restoreBackupLauncher` с переданным `DocumentsContract.EXTRA_INITIAL_URI`, автоматически открывая проводник сразу в папке `Documents/TIRUp/Backups/`.

### Этап 2: [Completed] Актуализация превью всех 7 виджетов рабочего стола (`drawable-nodpi/`)
- Полностью перерисованы все 7 растровых превью виджетов с суперсемплингом 3x (`SCALE = 3`) и сглаживанием LANCZOS в точном соответствии с эталонным стилем `widget_strip_preview.png`:
  - `widget_2x1_preview.png` (252 x 130)
  - `widget_3x1_preview.png` (395 x 130)
  - `widget_compact_preview.png` (248 x 247)
  - `widget_dashboard_medium_preview.png` (390 x 247)
  - `widget_dashboard_preview.png` (534 x 248)
  - `widget_minimal_preview.png` (144 x 144)
  - `widget_vertical_preview.png` (104 x 248)
- В превью виджетов обновлены показатели: актуальный сахар `6.5 → -0.1`, TIR `92%`, суточный баланс Компенсатора `+1ч 15м`, активный инсулин `💉 1.2 U` и стрик `🔥 7 д.`.
- Удалены устаревшие элементы (время `+7м`, слот батарейки `[battery] ?`).
## 21. Итерация 21: [Completed] Автовосстановление стрика, полный бэкап настроек и Главный экран по центру

### Этап 1: [Completed] Автовосстановление суточных сводок и стрика (`GlucoseRepositoryImpl.kt`, `TirupApplication.kt`, `AutoBackupManager.kt`)
- `GlucoseRepository.kt` & `GlucoseRepositoryImpl.kt`:
  - Добавлен метод `ensureDailySummariesUpToDate()`: автоматически анализирует временной диапазон всех имеющихся в базе замеров `glucose_readings` (`getEarliestTimestamp()` .. `getLatestTimestamp()`), и если количество сводок в `daily_summaries` меньше ожидаемого количества дней, автоматически пересчитывает и пакетно сохраняет `DailySummaryEntity` за все дни.
  - В `getStreakDays()` добавлен умный фоновый триггер: при количестве записей сводок <= 2 автоматически запускается фоновый расчет по всем историческим точкам базы. Как только сводки сохраняются в Room, Flow обновляет стрик прямо в UI без перезапуска приложения.
- `TirupApplication.kt`:
  - При старте приложения вызывается `glucoseRepository.ensureDailySummariesUpToDate()` и активируется `isWeeklyDigestEnabled = true`.
- `AutoBackupManager.kt`:
  - Добавлен вспомогательный метод `recalculateSummariesAfterRestore(database)` с автоматическим вызовом после любого восстановления (из локального автобэкапа, ZIP-архива, JSON или CSV).

### Этап 2: [Completed] Полная сериализация настроек (`AutoBackupManager.kt`)
- В методы `writeSettingsObject` и `parseSettings` включены ранее отсутствовавшие настройки:
  - `isWeeklyDigestEnabled` и `dismissedWeeklyDigestTimestamp` (еженедельный воскресный дайджест).
  - `bestStreakDays` и `lastStreakCelebratedDays` (рекорд и празднование стрика).
  - `showTreatmentsOnChart` и `showPredictionOnChart` (отображение уколов/углеводов и прогнозной траектории на графике сахара).
  - `metricsOrder` и `hiddenMetrics` (пользовательский порядок и видимость плиток метрик).

### Этап 3: [Completed] Главный экран по центру и эргономичная навигация (`MainActivity.kt`)
- Порядок страниц в `HorizontalPager` перестроен по модели «Hub & Spoke»:
  - **Страница 0 (Слева)**: `TrendsScreen` (Тренды, суточные профили, дайджест).
  - **Страница 1 (Центр - Home)**: `FocusScreen` (Главный экран мониторинга сахара, график, компенсатор).
  - **Страница 2 (Справа)**: `ReportsScreen` (Медицинские отчёты, AGP, HbA1c, выписка).
- Пейджер стартует со страницы 1 (`initialPage = 1`).
- Перенастроены события навигации: клик по уведомлениям сахара и виджетам ведет на страницу 1 (`Focus`), а клик по уведомлению дайджеста — на страницу 0 (`Trends`).
- В нижней панели навигации центральная кнопка Главного экрана получила акцентное оформление (увеличенный размер 50x30 dp, иконка 22 dp с мягкой подсветкой).

## 22. Итерация 22: [Completed] Согласование BLE-моста, SOS опекуна, таймеры и руководство пользователя

### Этап 1: [Completed] Журнал тревог и быстрые паузы (`FocusScreen.kt`)
- Убран эмоджи `⏸️` с неактивной кнопки паузы в журнале тревог (сохранён только для активного состояния паузы в статус-блоке).
- Убран лишний пробел между числом и единицами: «Пауза 1ч», «Пауза 15м» вместо «Пауза 1 ч».

### Этап 2: [Completed] Экстренное SOS и режим опекуна (`SettingsScreen.kt`)
- Роль в блоке «Экстренное SMS» полностью отвязана от BLE-моста: добавлен явный взаимоисключающий селектор роли устройства («Пациент / Подопечный» или «Опекун / Наблюдатель»).
- Для режима опекуна интерфейс оптимизирован: скрыты лишние кнопки пациента, поля переименованы в «Номер телефона подопечного (отправителя)» и «Запасной номер подопечного (имя/номер второго подопечного)».
- Кнопка «🔊 Проверить сирену и экран опекуна» дополнена 5-секундным таймером обратного отсчёта («⏳ Блокируйте экран (5с)...»), позволяющим опекуну заблокировать телефон для проверки пробуждения из сна.

### Этап 3: [Completed] Согласование состояний BLE-моста (`FocusScreen.kt`, `FocusViewModel.kt`, `SettingsScreen.kt`, `SettingsViewModel.kt`)
- Устранена рассинхронизация кнопки «Отключить мост» на главном экране и плашки «Выкл» в Настройках:
  - Введён единый предикат активности `isBridgeActive = isEnabled && role != BleBridgeRole.DISABLED`.
  - Выбор «✖️ Выкл» в Настройках устанавливает `role = DISABLED` и `isEnabled = false`.
  - Нажатие «Отключить мост» в диалоге главного экрана переводит состояние в `role = DISABLED, isEnabled = false` с остановкой вещания и сканера.
  - При отключённом мосте бейдж на главном экране показывает серую перечёркнутую иконку, заголовок диалога — «BLE-мост: Отключен», а кнопка предлагает «Включить мост».

### Этап 4: [Completed] Справочник CGM и Руководство пользователя (`GuidebookPdfGenerator.kt`, `UserManualPdfGenerator.kt`, `README.md`)
- В «Справочник параметров CGM» добавлены расширенные клинические нормы и диапазоны ATTD/ADA.
- В Главу 1 Руководства пользователя добавлено техническое сравнение технологий:
  - Автономный широковещательный радиомаяк TIRUp (Broadcast без сопряжения, <1% батареи, свободный BT-канал для помпы и часов).
  - Постоянный двусторонний сокет Juggluco (Connected RFCOMM/Wi-Fi) с гарантией доставки ACK и выкачкой всей истории SQLite.
- В `README.md` актуализировано описание трёхстраничного руководства пользователя.

